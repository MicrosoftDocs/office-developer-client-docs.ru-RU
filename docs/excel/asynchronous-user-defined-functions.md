---
title: Асинхронные пользовательские функции
manager: soliver
ms.date: 11/16/2014
ms.audience: Developer
ms.topic: overview
localization_priority: Normal
ms.assetid: 142eb27e-fb6f-4da3-bfb7-a88115bbb5d5
description: 'Область применения: Excel 2013 | Office 2013 | Visual Studio'
ms.openlocfilehash: 7fdf3bd09914981865c911fd65a78d044ad582f4
ms.sourcegitcommit: 8657170d071f9bcf680aba50b9c07f2a4fb82283
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/28/2019
ms.locfileid: "33412312"
---
# <a name="asynchronous-user-defined-functions"></a><span data-ttu-id="97254-103">Асинхронные пользовательские функции</span><span class="sxs-lookup"><span data-stu-id="97254-103">Asynchronous user-defined functions</span></span>

<span data-ttu-id="97254-104">**Область применения:** Excel 2013 | Office 2013 | Visual Studio</span><span class="sxs-lookup"><span data-stu-id="97254-104">**Applies to**: Excel 2013 | Office 2013 | Visual Studio</span></span> 
  
<span data-ttu-id="97254-105">Microsoft Excel 2013 может асинхронно вызывать пользовательские функции.</span><span class="sxs-lookup"><span data-stu-id="97254-105">Microsoft Excel 2013 can call user-defined functions asynchronously.</span></span> <span data-ttu-id="97254-106">Асинхронный вызов функций может увеличить производительность, допуская одновременное выполнение нескольких вычислений.</span><span class="sxs-lookup"><span data-stu-id="97254-106">Calling functions asynchronously can improve performance by allowing several calculations to run at the same time.</span></span> <span data-ttu-id="97254-107">При выполнении пользовательских функций в вычислительном кластере асинхронный вызов функций позволяет использовать в вычислениях несколько компьютеров.</span><span class="sxs-lookup"><span data-stu-id="97254-107">When you run user-defined functions on a compute cluster, calling functions asynchronously enables several computers to be used to complete the calculations.</span></span>
  
## <a name="when-to-use-asynchronous-user-defined-functions"></a><span data-ttu-id="97254-108">Когда использовать асинхронные пользовательские функции</span><span class="sxs-lookup"><span data-stu-id="97254-108">When to use asynchronous user-defined functions</span></span>

<span data-ttu-id="97254-109">Некоторые пользовательские функции должны ожидать внешние ресурсы.</span><span class="sxs-lookup"><span data-stu-id="97254-109">Some user-defined functions must wait for external resources.</span></span> <span data-ttu-id="97254-110">Во время ожидания поток вычислений Excel блокируется.</span><span class="sxs-lookup"><span data-stu-id="97254-110">While they wait, the Excel calculation thread is blocked.</span></span> <span data-ttu-id="97254-111">В Excel 2013 пользовательские функции могут выполняться асинхронно.</span><span class="sxs-lookup"><span data-stu-id="97254-111">In Excel 2013, user-defined functions can run asynchronously.</span></span> <span data-ttu-id="97254-112">При этом поток вычислений освобождается для выполнения других вычислений во время ожидания пользовательской функции.</span><span class="sxs-lookup"><span data-stu-id="97254-112">This frees the calculation thread to run other calculations while the user-defined function waits.</span></span>
  
<span data-ttu-id="97254-113">В Excel 2007 программисты могут одновременно запускать несколько пользовательских функций, увеличивая количество потоков, используемых в пересчетах нескольких потоков.</span><span class="sxs-lookup"><span data-stu-id="97254-113">In Excel 2007, programmers could run multiple user-defined functions at the same time by increasing the number of threads used in multiple-thread recalculations.</span></span> <span data-ttu-id="97254-114">Этот метод имеет недостатки, в основном потому, что количество потоков является параметром, ограниченным приложением, и его невозможно контролировать на уровне одной функции или надстройки.</span><span class="sxs-lookup"><span data-stu-id="97254-114">This method has drawbacks primarily because the number of threads is a setting scoped to an application and cannot be controlled at the level of a single function or an add-in.</span></span>
  
<span data-ttu-id="97254-115">Программисты должны использовать асинхронные вызовы пользовательских функций, когда функция должна ожидать внешние ресурсы.</span><span class="sxs-lookup"><span data-stu-id="97254-115">Programmers should use asynchronous user-defined function calls when the function must wait for external resources.</span></span> <span data-ttu-id="97254-116">Например, функция, которая отправляет SOAP SOAP запрос через Интернет, должна ждать, пока сеть не получит запрос, удаленный сервер для выполнения запроса, а сеть возвращает результат.</span><span class="sxs-lookup"><span data-stu-id="97254-116">For example, a function that sends a SOAP request over the Internet must wait for the network to deliver the request, the remote server to complete the request, and the network to return the result.</span></span> <span data-ttu-id="97254-117">В этом случае не происходит значительных вычислений, и Excel может продолжить работу с другими вычислениями.</span><span class="sxs-lookup"><span data-stu-id="97254-117">In this case, there is no significant computing occurring and Excel can continue with other calculations.</span></span>
  
<span data-ttu-id="97254-118">При отправке запросов функцией в вычислительный кластер программисты также могут использовать асинхронные пользовательские функции.</span><span class="sxs-lookup"><span data-stu-id="97254-118">Programmers can also use asynchronous user-defined functions when a function is sending requests to a compute cluster.</span></span> <span data-ttu-id="97254-119">В этом случае время ожидания в сети не требуется, но кластер может выполнять отдельные вызовы на отдельных серверах.</span><span class="sxs-lookup"><span data-stu-id="97254-119">In this case, there is not only network latency to wait for, but the cluster can execute separate calls on separate servers.</span></span> <span data-ttu-id="97254-120">Для повышения производительности вызовы могут быть перекрывающиеся, не дожидаясь завершения каждого вызова.</span><span class="sxs-lookup"><span data-stu-id="97254-120">By not waiting for each call to finish, the calls can be overlapped to improve performance.</span></span> <span data-ttu-id="97254-121">В некоторых случаях это улучшение является существенным.</span><span class="sxs-lookup"><span data-stu-id="97254-121">In some cases this improvement is significant.</span></span>
  
> [!NOTE]
> <span data-ttu-id="97254-122">Пользовательские функции невозможно зарегистрировать как асинхронный и надежный в кластере.</span><span class="sxs-lookup"><span data-stu-id="97254-122">User-defined functions cannot be registered as both asynchronous and cluster safe.</span></span> 
  
## <a name="writing-an-asynchronous-user-defined-function"></a><span data-ttu-id="97254-123">Написание асинхронной пользовательской функции</span><span class="sxs-lookup"><span data-stu-id="97254-123">Writing an asynchronous user-defined function</span></span>

<span data-ttu-id="97254-124">Асинхронные пользовательские функции должны отслеживать дескриптор и использовать этот дескриптор при заполнении Excel, когда вызов функции завершен.</span><span class="sxs-lookup"><span data-stu-id="97254-124">Asynchronous user-defined functions must keep track of a handle and use that handle when informing Excel that the function call is finished.</span></span> <span data-ttu-id="97254-125">Асинхронная пользовательская функция разделяется на две части.</span><span class="sxs-lookup"><span data-stu-id="97254-125">An asynchronous user-defined function is split into two pieces.</span></span> <span data-ttu-id="97254-126">Первый фрагмент — это стандартная точка входа UDF, которая запустит вторую, отдельно асинхронную операцию.</span><span class="sxs-lookup"><span data-stu-id="97254-126">The first piece is the standard UDF entry point, which will launch a second, separate asynchronous operation.</span></span> <span data-ttu-id="97254-127">Обратные вызовы в Excel следует вносить во время точки входа UDF.</span><span class="sxs-lookup"><span data-stu-id="97254-127">Callbacks into Excel should be made during the UDF entry point.</span></span> <span data-ttu-id="97254-128">После этого первая начальная часть функции возвращает управление потоком вычислений в Excel, что позволяет продолжить вычисление.</span><span class="sxs-lookup"><span data-stu-id="97254-128">The first launching portion of the function will then return control of its calculation thread to Excel, which will continue calculation.</span></span> <span data-ttu-id="97254-129">По завершении второй асинхронной операции она должна вызвать Excel и предоставить ей результат.</span><span class="sxs-lookup"><span data-stu-id="97254-129">When the second asynchronous operation is complete, it must call back into Excel and provide Excel with its result.</span></span> 
  
> [!NOTE]
> <span data-ttu-id="97254-130">Все аргументы, переданные в UDF, необходимые асинхронной части, функция должна быть глубоко скопирована, так как Excel освобождает эти аргументы при возврате точки входа UDF.</span><span class="sxs-lookup"><span data-stu-id="97254-130">Any arguments passed into the UDF that are needed by the asynchronous portion the function must be deep copied because Excel frees these arguments when the UDF entry point returns.</span></span> 
  
<span data-ttu-id="97254-131">Excel предоставляет набор событий, которые может использовать надстройка XLL для управления жизненным циклом асинхронных вызовов UDF.</span><span class="sxs-lookup"><span data-stu-id="97254-131">Excel provides a set of events that an XLL add-in can use in order to manage the life cycle of asynchronous UDF calls.</span></span> <span data-ttu-id="97254-132">Эти события указывают на то, что Excel завершается с вычислениями или что вычисление отменено.</span><span class="sxs-lookup"><span data-stu-id="97254-132">These events indicate that Excel is finished with calculations or that the calculation was canceled.</span></span>
  
### <a name="declaring-an-asynchronous-function"></a><span data-ttu-id="97254-133">Объявление асинхронной функции</span><span class="sxs-lookup"><span data-stu-id="97254-133">Declaring an asynchronous function</span></span>

<span data-ttu-id="97254-134">Асинхронные пользовательские функции необходимо объявить асинхронно, если они зарегистрированы.</span><span class="sxs-lookup"><span data-stu-id="97254-134">You must declare asynchronous user-defined functions as asynchronous when they are registered.</span></span> <span data-ttu-id="97254-135">Для этого добавляется параметр, указывающий на структуру, представленную "X" в строке типа регистрации, в любом месте списка параметров UDF.</span><span class="sxs-lookup"><span data-stu-id="97254-135">This is performed by adding a parameter that points to a XLOPER12 structure, represented by "X" in the registration type string, anywhere in the list of UDF parameters.</span></span> <span data-ttu-id="97254-136">Excel использует этот параметр, чтобы передать маркер асинхронного вызова.</span><span class="sxs-lookup"><span data-stu-id="97254-136">Excel uses this parameter to pass the asynchronous call handle.</span></span> <span data-ttu-id="97254-137">Надстройка XLL должна передать маркер асинхронного вызова и результат функции обратно в Excel, когда результат будет готов.</span><span class="sxs-lookup"><span data-stu-id="97254-137">The XLL add-in must pass the asynchronous call handle and the result of the function back to Excel when the result is ready.</span></span> <span data-ttu-id="97254-138">Кроме того, тип возвращаемого значения UDF должен быть **void**, обозначенным "_гт_", как первый символ в строке типа.</span><span class="sxs-lookup"><span data-stu-id="97254-138">In addition, the return type of the UDF should be **void**, designated by ">" as the first character in the type string.</span></span> <span data-ttu-id="97254-139">Возвращаемый тип — void, так как синхронная часть UDF не возвращает значение в Excel.</span><span class="sxs-lookup"><span data-stu-id="97254-139">The return type is void because the synchronous part of the UDF does not return a value to Excel.</span></span> <span data-ttu-id="97254-140">Вместо этого надстройка XLL возвращает значение асинхронно через обратный вызов.</span><span class="sxs-lookup"><span data-stu-id="97254-140">Instead, the XLL add-in returns a value asynchronously through a callback.</span></span> 
  
<span data-ttu-id="97254-141">Асинхронные функции можно объявить потокобезопасными, а затем синхронный компонент UDF используется при многопотоковой пересчете.</span><span class="sxs-lookup"><span data-stu-id="97254-141">You can declare asynchronous functions as thread-safe and then the synchronous part of the UDF is used in a multi-threaded recalculation.</span></span> 
  
<span data-ttu-id="97254-142">В следующем примере кода показана асинхронная пользовательская функция, зарегистрированная с\>помощью "ККС" в качестве строки типа регистрации:</span><span class="sxs-lookup"><span data-stu-id="97254-142">The following code example shows an asynchronous user-defined function registered by using "\>QX" as the registration type string:</span></span>
  
```cpp
void MyAsyncUDF(LPXLOPER12 arg1, LPXLOPER12 pxAsyncHandle)
{
…
}
```

### <a name="returning-values"></a><span data-ttu-id="97254-143">Возвращаемые значения</span><span class="sxs-lookup"><span data-stu-id="97254-143">Returning values</span></span>

<span data-ttu-id="97254-144">Когда результат асинхронного вызова готов, надстройка XLL возвращает результат в Excel, выполняя обратный вызов типа [ксласинкретурн](xlasyncreturn.md).</span><span class="sxs-lookup"><span data-stu-id="97254-144">When the result of the asynchronous call is ready, the XLL add-in returns the result to Excel by performing a callback of type [xlAsyncReturn](xlasyncreturn.md).</span></span>
  
<span data-ttu-id="97254-145">**ксласинкретурн** — единственный обратный вызов, который можно использовать в потоках, не являющихся вычислениями, во время пересчета.</span><span class="sxs-lookup"><span data-stu-id="97254-145">**xlAsyncReturn** is the only callback you can use on non-calculation threads during recalculation.</span></span> <span data-ttu-id="97254-146">Таким образом, асинхронная часть асинхронной UDF не должна выполнять другие обратные вызовы.</span><span class="sxs-lookup"><span data-stu-id="97254-146">Therefore, the asynchronous part of an asynchronous UDF should not perform any other callbacks.</span></span> 
  
### <a name="handling-events"></a><span data-ttu-id="97254-147">Обработка событий</span><span class="sxs-lookup"><span data-stu-id="97254-147">Handling events</span></span>

<span data-ttu-id="97254-148">Начиная с Excel 2010 XLL могут получать события, предназначенные для управления жизненным циклом асинхронной функции.</span><span class="sxs-lookup"><span data-stu-id="97254-148">Starting in Excel 2010, XLLs can receive events designed to manage the asynchronous function life cycle.</span></span> <span data-ttu-id="97254-149">Более подробную информацию можно узнать в статье [Обработка событий](handling-events.md).</span><span class="sxs-lookup"><span data-stu-id="97254-149">For more information, see [Handling Events](handling-events.md).</span></span>
  
## <a name="see-also"></a><span data-ttu-id="97254-150">См. также</span><span class="sxs-lookup"><span data-stu-id="97254-150">See also</span></span>

- [<span data-ttu-id="97254-151">Разработка XLL-файлов для Excel</span><span class="sxs-lookup"><span data-stu-id="97254-151">Developing Excel XLLs</span></span>](developing-excel-xlls.md)

