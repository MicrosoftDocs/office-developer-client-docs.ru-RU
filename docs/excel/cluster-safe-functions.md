---
title: Безопасные функции кластера
manager: soliver
ms.date: 11/16/2014
ms.audience: Developer
ms.topic: reference
localization_priority: Normal
ms.assetid: 787badaf-8782-454d-a016-7eae83bbd8a9
description: 'Область применения: Excel 2013 | Office 2013 | Visual Studio'
ms.openlocfilehash: d32925fcd5c3be7fe3e615ee2290f25c7595911c
ms.sourcegitcommit: 8657170d071f9bcf680aba50b9c07f2a4fb82283
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/28/2019
ms.locfileid: "33409302"
---
# <a name="cluster-safe-functions"></a><span data-ttu-id="595ed-103">Безопасные функции кластера</span><span class="sxs-lookup"><span data-stu-id="595ed-103">Cluster safe functions</span></span>

<span data-ttu-id="595ed-104">**Область применения:** Excel 2013 | Office 2013 | Visual Studio</span><span class="sxs-lookup"><span data-stu-id="595ed-104">**Applies to**: Excel 2013 | Office 2013 | Visual Studio</span></span> 
  
<span data-ttu-id="595ed-105">В Excel 2013 Excel позволяет разгрузить вызовы функции пользовательской функции (UDF) в кластер высокопроизводительных вычислительных систем через выделенный интерфейс соединителя кластера.</span><span class="sxs-lookup"><span data-stu-id="595ed-105">In Excel 2013, Excel can offload User-Defined Function (UDF) calls to a high-performance computing cluster through a dedicated cluster connector interface.</span></span> <span data-ttu-id="595ed-106">Compute Clusters поставщики предоставляют соединители кластера.</span><span class="sxs-lookup"><span data-stu-id="595ed-106">Compute cluster vendors provide Cluster Connectors.</span></span> <span data-ttu-id="595ed-107">Авторы UDF могут объявлять свои пользовательские функции как безопасные кластеры, а затем, когда присутствует соединитель кластера, Excel отправляет вызовы этих пользовательских функций в соединитель кластера для разгрузки.</span><span class="sxs-lookup"><span data-stu-id="595ed-107">UDF authors can declare their UDFs as cluster safe and then, when a cluster connector is present, Excel sends calls to these UDFs to the cluster connector for offloading.</span></span>
  
<span data-ttu-id="595ed-108">Когда Excel обнаруживает в ходе пересчета ФАЙЛовую группу UDF, которая выполняется в данный момент, она передает имя модуля UDF, надежного для кластера, и все параметры соединителя кластера.</span><span class="sxs-lookup"><span data-stu-id="595ed-108">When Excel discovers a cluster-safe UDF during recalculation, it passes the name of the XLL that is currently running, the name of the cluster-safe UDF, and any parameters to the cluster connector.</span></span> <span data-ttu-id="595ed-109">Соединитель выполняет удаленный вызов UDF и возвращает результаты в Excel.</span><span class="sxs-lookup"><span data-stu-id="595ed-109">The connector runs the UDF call remotely and returns the results to Excel.</span></span> <span data-ttu-id="595ed-110">Независимое вычисление продолжается, и когда соединитель кластера закончил выполнение UDF, он передает результаты в Excel и зависимые вычисления продолжаются.</span><span class="sxs-lookup"><span data-stu-id="595ed-110">Non-dependent calculation continues and when the cluster connector has finished running the UDF, it passes the results to Excel and dependent calculations continue.</span></span> <span data-ttu-id="595ed-111">Механизм этого асинхронного поведения имитирует механизм, используемый асинхронными функциями UDF, за исключением того, что соединитель кластера управляет асинхронными аспектами, а не автором UDF.</span><span class="sxs-lookup"><span data-stu-id="595ed-111">The mechanism for this asynchronous behavior mimics the mechanism used by asynchronous UDFs, except that the cluster connector manages the asynchronous aspects instead of the UDF author.</span></span> <span data-ttu-id="595ed-112">Как правило, в кластерном соединителе реализована оболочка XLL для загрузки XLL и запуска пользовательских функций на узлах вычислительного кластера.</span><span class="sxs-lookup"><span data-stu-id="595ed-112">Typically, a cluster connector implements an XLL shim to load XLLs and run UDFs on compute cluster nodes.</span></span>
  
<span data-ttu-id="595ed-113">Механизм объявления пользовательских функций в качестве надежного кластера похож на объявления пользовательских функций, которые являются безопасными для пересчета с несколькими потоками.</span><span class="sxs-lookup"><span data-stu-id="595ed-113">The mechanics of declaring UDFs as cluster-safe resemble those of declaring UDFs as safe for multi-threaded recalculation.</span></span> <span data-ttu-id="595ed-114">Тем не менее, поскольку UDF не обязательно выполняется на том же компьютере, что и другие функции UDF из того же сеанса Excel, существуют различные рекомендации по написанию пользовательских функций, не защищенных с помощью кластера.</span><span class="sxs-lookup"><span data-stu-id="595ed-114">However, because the UDF is not necessarily running on the same computer as other UDFs from the same Excel session, there are different considerations when writing cluster-safe UDFs.</span></span>
  
<span data-ttu-id="595ed-115">Чтобы зарегистрировать UDF в качестве надежной кластеризации, необходимо вызвать функцию обратного вызова [xlfRegister (Form 1)](xlfregister-form-1.md) с помощью интерфейса **Excel12** или **Excel12v** .</span><span class="sxs-lookup"><span data-stu-id="595ed-115">To register a UDF as cluster-safe, you must call the [xlfRegister (Form 1)](xlfregister-form-1.md) callback function through the **Excel12** or **Excel12v** interface.</span></span> <span data-ttu-id="595ed-116">Дополнительные сведения об этих интерфейсах приведены в статье [Excel4/Excel12](excel4-excel12.md) and [Excel4v/Excel12v](excel4v-excel12v.md).</span><span class="sxs-lookup"><span data-stu-id="595ed-116">For more information about these interfaces, see the [Excel4/Excel12](excel4-excel12.md) and [Excel4v/Excel12v](excel4v-excel12v.md).</span></span> <span data-ttu-id="595ed-117">Регистрация UDF в качестве безопасной для кластеризации через интерфейс **Excel4** или **Excel4v** не поддерживается.</span><span class="sxs-lookup"><span data-stu-id="595ed-117">Registering a UDF as cluster-safe through the **Excel4** or **Excel4v** interface is not supported.</span></span> 
  
<span data-ttu-id="595ed-118">Если вы регистрируете функцию как надежная Кластеризация, необходимо убедиться в том, что функция работает в кластерной безопасном способе.</span><span class="sxs-lookup"><span data-stu-id="595ed-118">If you register a function as cluster-safe, you must ensure that the function behaves in a cluster-safe way.</span></span> <span data-ttu-id="595ed-119">Несмотря на то, что точное поведение соединителя кластера зависит от реализации, необходимо разрабатывать UDF для работы на распределенном компьютере и иметь следующие характеристики:</span><span class="sxs-lookup"><span data-stu-id="595ed-119">Although the exact behavior of the cluster connector is implementation-specific, you should design your UDF to run on a distributed computer system and to have the following characteristics:</span></span>
  
- <span data-ttu-id="595ed-120">UDF не должно зависеть от состояния памяти.</span><span class="sxs-lookup"><span data-stu-id="595ed-120">A UDF should not rely on any memory state.</span></span> <span data-ttu-id="595ed-121">Например, UDF не должно опираться на существующий кэш в памяти.</span><span class="sxs-lookup"><span data-stu-id="595ed-121">For example, a UDF should not rely on an existing in-memory cache.</span></span>
    
- <span data-ttu-id="595ed-122">ФАЙЛовая система UDF не должна выполнять обратные вызовы Excel, которые не поддерживаются поставщиком соединителей кластера.</span><span class="sxs-lookup"><span data-stu-id="595ed-122">A UDF should not perform Excel callbacks that the cluster connector provider does not support.</span></span>
    
<span data-ttu-id="595ed-123">В дополнение к принципу работы в кластере существуют следующие технические ограничения для функций, безопасных в кластерах:</span><span class="sxs-lookup"><span data-stu-id="595ed-123">In addition to cluster-safe behavior, there are the following technical restrictions on cluster-safe UDFs:</span></span>
  
1. <span data-ttu-id="595ed-124">Не указаны аргументы XLOPER (Types ' P ', ' R ').</span><span class="sxs-lookup"><span data-stu-id="595ed-124">No XLOPER arguments (types 'P', 'R').</span></span>
    
2. <span data-ttu-id="595ed-125">Нет аргументов XLOPER12, которые поддерживают ссылки на диапазоны (тип ' U ').</span><span class="sxs-lookup"><span data-stu-id="595ed-125">No XLOPER12 arguments that support range references (type 'U').</span></span>
    
3. <span data-ttu-id="595ed-126">Не может быть эквивалентной функции листа макросов (' # '&amp;и ' ' не может быть объединено).</span><span class="sxs-lookup"><span data-stu-id="595ed-126">Cannot be a macro sheet equivalent function ('#' and '&amp;' cannot be combined).</span></span>
    
<span data-ttu-id="595ed-127">Если вы используете UDF с более коротким временем выполнения, затраты на разгрузку могут быть больше, чем время, требуемое для выполнения UDF, что позволяет инвертировать многие преимущества использования этой инфраструктуры.</span><span class="sxs-lookup"><span data-stu-id="595ed-127">For UDFs with shorter execution times, the overhead of offloading may be larger than the time it takes the UDF to execute, negating many of the benefits of using this infrastructure.</span></span>
  
> [!NOTE]
> <span data-ttu-id="595ed-128">Вы не можете объявить UDF, защищенную с помощью кластера, в качестве асинхронной UDF.</span><span class="sxs-lookup"><span data-stu-id="595ed-128">You cannot declare a cluster-safe UDF as an asynchronous UDF.</span></span> 
  
<span data-ttu-id="595ed-129">С помощью UDF можно определить, выполняется ли он с помощью соединителя кластера, вызвав функцию обратного вызова [кслруннингонклустер](xlrunningoncluster.md) .</span><span class="sxs-lookup"><span data-stu-id="595ed-129">A UDF can determine whether it is being run using a cluster connector by calling the [xlRunningOnCluster](xlrunningoncluster.md) callback function.</span></span> 
  

