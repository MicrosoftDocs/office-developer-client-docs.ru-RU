---
title: Действия макроса ImportExportSpreadsheet
TOCTitle: ImportExportSpreadsheet Macro Action
ms:assetid: 526aef41-8329-5335-9d16-4d332542a297
ms:mtpsurl: https://msdn.microsoft.com/library/Ff193927(v=office.15)
ms:contentKeyID: 48544846
ms.date: 09/18/2015
mtps_version: v=office.15
f1_keywords:
- vbaac10.chm31446
f1_categories:
- Office.Version=v15
ms.openlocfilehash: 78819ba2a82bf2fc9ab5c39300d06c1acdda80bc
ms.sourcegitcommit: c557bbcccf37a6011f89aae1ddd399dfe549d087
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 10/31/2018
ms.locfileid: "25875807"
---
# <a name="importexportspreadsheet-macro-action"></a>Действия макроса ImportExportSpreadsheet


**Применимо к**: Access 2013, Office 2013

Действие **ImportExportSpreadsheet** можно использовать для импорта или экспорта данных между текущей базы данных Access (MDB- или .accdb) или проект Microsoft Access (файлы с расширением ADP) и файла электронной таблицы. Также можно связать данные в электронную таблицу Microsoft Excel для текущей базы данных Microsoft Access. Связывания электронной таблицы можно просматривать и изменять данные электронной таблицы с доступом по-прежнему предоставляя полный доступ к данным из программы электронной таблицы Excel. Также можно связать с данным в файл электронной таблицы Lotus 1-2-3, но эти данные в доступа только для чтения.


> [!NOTE]
> <P>
		Эта действие не разрешено, если база данных не является доверенной. Дополнительные сведения о включении макросов см. по ссылкам в разделе See Also этой статьи.
</P>



## <a name="setting"></a>Параметр

**ПреобразоватьЭлектроннуюТаблицу** имеет следующие аргументы.

<table>
<colgroup>
<col style="width: 50%" />
<col style="width: 50%" />
</colgroup>
<thead>
<tr class="header">
<th><p>Аргумент макрокоманды</p></th>
<th><p>Описание</p></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><p><strong>Тип передачи</strong></p></td>
<td><p>Тип переноса, который требуется сделать. Выберите <strong>Импорт</strong>, <strong>Экспорт</strong>или <strong>ссылку</strong> в поле <strong>Тип преобразования</strong> в разделе <strong>Действие аргументы</strong> в области построения макросов. Значение по умолчанию — <strong>Импорт</strong>.</p>

> [!NOTE]
> <P>Тип передачи <STRONG>связи</STRONG> не поддерживается для проектов Access (файлы с расширением ADP).</P>


<p></p></td>
</tr>
<tr class="even">
<td><p><strong>Тип электронной таблицы</strong></p></td>
<td><p>Тип электронной таблицы для импорта, экспорта или ссылка на. В поле можно выбрать один из нескольких типов электронной таблицы. Значение по умолчанию — <strong>Книги Excel</strong>.</p>

> [!NOTE]
> <P>Можно импортировать из и ссылка на Lotus (только для чтения). Файлы WK4, но не может экспортировать доступ к данным в данном формате электронной таблицы. Access больше не поддерживает импорт, экспорт или связывание данных из Lotus. WKS или электронные таблицы Excel версии 2.0 с помощью этого действия. Если вы хотите импортировать из или ссылка на данные электронной таблицы в Excel версии 2.0 или Lotus. WKS форматирование, преобразование данных электронных таблиц в более поздней версии Excel или Lotus 1-2-3 перед Импорт или связывание данных в Access.</P>


<p></p></td>
</tr>
<tr class="odd">
<td><p><strong>Имя таблицы</strong></p></td>
<td><p>Имя таблицы Access Импорт данных электронных таблиц для экспорта данных электронной таблицы из или связывание данных электронной таблицы. Также можно ввести имя доступа выберите запрос, который необходимо экспортировать данные из. Обязательный аргумент. При выборе <strong>импорта</strong> в аргументе <strong>Тип</strong> доступа добавляет данным электронных таблиц в этой таблице, если таблица уже существует. В противном случае создается новая таблица с данными электронной таблицы. В клиенте нельзя использовать инструкции SQL для указания данных для экспорта при использовании <strong>ImportExportSpreadsheet</strong> действие. Вместо использования инструкции SQL, необходимо сначала создать запрос и затем указать имя запроса в качестве аргумента <strong>Имя таблицы</strong> .</p></td>
</tr>
<tr class="even">
<td><p><strong>Имя файла</strong></p></td>
<td><p>Имя файла электронной таблицы для импорта, экспорта или ссылка на. Включите полный путь. Обязательный аргумент. Access создает новую таблицу при экспорте данных из Access. Если имя файла совпадает с именем существующей электронной таблицы, Access заменяет существующей электронной таблицы, если не экспорте книги 5.0 или более поздней версии Excel. В этом случае Access копирует экспортируемых данных доступен следующий нового листа в книге. При импорте из или ссылки на Excel версий 5.0 или более поздних версий, определенного рабочего листа можно указать с помощью аргумент <strong>диапазон</strong> .</p></td>
</tr>
<tr class="odd">
<td><p><strong>Содержит имена полей</strong></p></td>
<td><p>Указывает, содержит ли первой строки электронной таблицы имена полей. Если выбран вариант <strong>Да</strong>, Access использует имена в этой строке как имена полей таблицы Microsoft Access при Импорт или связывание данных электронной таблицы. Если выбран вариант <strong>Нет</strong>, Access рассматривается как обычная строка данных первой строки. Значение по умолчанию: <strong>Нет</strong>. При экспорте таблицы Microsoft Access или выборку в электронную таблицу имена полей вставляется в первую строку электронной таблицы независимо от того, вы выбираете в этот аргумент.</p></td>
</tr>
<tr class="even">
<td><p><strong>Range</strong></p></td>
<td><p>Диапазон ячеек для импорта. Оставьте данный аргумент пустым, чтобы импортировать или связать всей таблицы. Можно ввести имя диапазона в электронной таблице или указать диапазон ячеек для импорта, такие как A1:E25 (Обратите внимание, что A1.. Синтаксис E25 не работает в Microsoft Access 97 или более поздней версии). При импорте из или ссылки на Excel версий 5.0 или более поздних версий, можно перед именем диапазона имя листа и восклицательный знак; Например, бюджет! A1:C7.</p>

> [!NOTE]
> <P>При экспорте в электронную таблицу, этот аргумент необходимо оставить пустым. Если ввести диапазон, экспорт завершится с ошибкой.</P>


<p></p></td>
</tr>
</tbody>
</table>


## <a name="remarks"></a>Замечания

Можно экспортировать данные в запросах доступа электронных таблиц. Microsoft Access экспортирует набор результатов запроса в виде таблицы.

Данные электронной таблицы, добавляемые в существующую таблицу Access должны быть совместимы со структурой таблицы.

  - Каждое поле в таблице должен быть типу данных, то соответствующее поле в таблице.

  - Поля должны быть в том же порядке (Если аргумент **Содержит имена полей** задано значение **Да**, в этом случае поле имена в электронной таблице должны совпадать имена полей в таблице).

Это действие аналогично выбрав вкладку **Внешних данных** и и выбрав пункты **Excel** в **Импорт** или **Экспорт** группу или **несколько** в группе **Импорт** или **Экспорт** и щелкнув **Файл Lotus 1-2-3**. Эти команды можно использовать для выбора источника данных, например Access или тип базы данных, электронную таблицу или текстовый файл. При выборе электронной таблице отображается ряд диалоговых окон или запускается мастер Microsoft Access, в котором выберите имя электронной таблицы и другие параметры. Аргументы действие **ImportExportSpreadsheet** соответствуют параметрам этих диалоговых окон или мастера.


> [!NOTE]
> <P>Запроса и фильтрации связанной электронной таблице запроса или фильтра с учетом регистра.</P>



При связывании таблицы Excel, которая открыта в режиме редактирования Access будет ждать в электронной таблице Excel — режим редактирования перед завершением ссылок; нет тайм-аут.

Чтобы выполнить действие **ImportExportSpreadsheet** в Visual Basic для приложений (VBA) модуль, используйте метод **ПреобразоватьЭлектроннуюТаблицу** **объекта** .

