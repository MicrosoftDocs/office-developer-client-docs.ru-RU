---
title: Объект Field (Ссылка на настольные базы данных)
TOCTitle: The Field object
ms:assetid: 55531e04-d74f-6394-df64-1660e5d572ca
ms:mtpsurl: https://msdn.microsoft.com/library/JJ249284(v=office.15)
ms:contentKeyID: 48544926
ms.date: 09/18/2015
mtps_version: v=office.15
localization_priority: Normal
ms.openlocfilehash: 2cbd5752399e5a14f08b7eb944e3a028ba53f561
ms.sourcegitcommit: 8fe462c32b91c87911942c188f3445e85a54137c
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/23/2019
ms.locfileid: "32314023"
---
# <a name="field-object"></a><span data-ttu-id="a75a1-102">Объект Field</span><span class="sxs-lookup"><span data-stu-id="a75a1-102">Field object</span></span>

<span data-ttu-id="a75a1-103">**Область применения**: Access 2013, Office 2013</span><span class="sxs-lookup"><span data-stu-id="a75a1-103">**Applies to**: Access 2013, Office 2013</span></span>

<span data-ttu-id="a75a1-104">Каждый **объект Field** обычно соответствует столбцу в таблице баз данных.</span><span class="sxs-lookup"><span data-stu-id="a75a1-104">Each **Field** object usually corresponds to a column in a database table.</span></span> <span data-ttu-id="a75a1-105">Однако поле **также может** представлять указатель на другой Набор **записей,** называемый главой.</span><span class="sxs-lookup"><span data-stu-id="a75a1-105">However, a **Field** can also represent a pointer to another **Recordset**, called a chapter.</span></span> <span data-ttu-id="a75a1-106">Исключения, такие как столбцы глав, будут освещаться в этом руководстве позже.</span><span class="sxs-lookup"><span data-stu-id="a75a1-106">Exceptions, such as chapter columns, will be covered later in this guide.</span></span>

<span data-ttu-id="a75a1-107">Используйте **свойство Value** объектов **Field** для набора или возврата данных для текущей записи.</span><span class="sxs-lookup"><span data-stu-id="a75a1-107">Use the **Value** property of **Field** objects to set or return data for the current record.</span></span> <span data-ttu-id="a75a1-108">В зависимости от функциональных возможностей, которые предоставляет поставщик, некоторые коллекции, методы или свойства объекта **Field** могут быть недоступны.</span><span class="sxs-lookup"><span data-stu-id="a75a1-108">Depending on the functionality the provider exposes, some collections, methods, or properties of a **Field** object may not be available.</span></span>

<span data-ttu-id="a75a1-109">С помощью коллекций, методов и свойств объекта **Field** можно сделать следующее:</span><span class="sxs-lookup"><span data-stu-id="a75a1-109">With the collections, methods, and properties of a **Field** object, you can do the following:</span></span>

- <span data-ttu-id="a75a1-110">Возвращаем имя поля с помощью свойства **Name.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-110">Return the name of a field by using the **Name** property.</span></span>

- <span data-ttu-id="a75a1-111">Просмотр или изменение данных в поле с помощью свойства **Value.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-111">View or change the data in the field by using the **Value** property.</span></span> <span data-ttu-id="a75a1-112">**Значение** — это свойство по умолчанию объекта **Field.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-112">**Value** is the default property of the **Field** object.</span></span>

- <span data-ttu-id="a75a1-113">Возвращаем основные характеристики поля с помощью свойств **Type,** **Precision** и **NumericScale.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-113">Return the basic characteristics of a field by using the **Type**, **Precision**, and **NumericScale** properties.</span></span>

- <span data-ttu-id="a75a1-114">Возврат объявленного размера поля с помощью свойства **DefinedSize.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-114">Return the declared size of a field by using the **DefinedSize** property.</span></span>

- <span data-ttu-id="a75a1-115">Возврат фактического размера данных в заданное поле с помощью **свойства ActualSize.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-115">Return the actual size of the data in a given field by using the **ActualSize** property.</span></span>

- <span data-ttu-id="a75a1-116">Определите, какие типы функций поддерживаются для данного поля с помощью коллекции свойств и **свойств** **Attributes.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-116">Determine what types of functionality are supported for a given field by using the **Attributes** property and **Properties** collection.</span></span>

- <span data-ttu-id="a75a1-117">Манипулировать значениями полей, содержащих длинные двоичные или длинные данные символов, с помощью методов **AppendChunk** и **GetChunk.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-117">Manipulate the values of fields containing long binary or long character data by using the **AppendChunk** and **GetChunk** methods.</span></span>

- <span data-ttu-id="a75a1-118">Устранение несоответствий значений полей при пакетном обновлении с помощью свойств **OriginalValue** и **UnderlyingValue,** если поставщик поддерживает пакетные обновления.</span><span class="sxs-lookup"><span data-stu-id="a75a1-118">Resolve discrepancies in field values during batch updating by using the **OriginalValue** and **UnderlyingValue** properties, if the provider supports batch updates.</span></span>

## <a name="describing-a-field"></a><span data-ttu-id="a75a1-119">Описание поля</span><span class="sxs-lookup"><span data-stu-id="a75a1-119">Describing a field</span></span>

<span data-ttu-id="a75a1-120">В последующих темах будут рассмотрены свойства объекта [Field,](field-object-ado.md) которые представляют информацию, описываемую сам объект **Field,** то есть метаданные о поле.</span><span class="sxs-lookup"><span data-stu-id="a75a1-120">The topics that follow will discuss properties of the [Field](field-object-ado.md) object that represent information that describes the **Field** object itself — that is, metadata about the field.</span></span> <span data-ttu-id="a75a1-121">Эти сведения можно использовать для определения схемы **наборов записей.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-121">This information can be used to determine much about the schema of the **Recordset**.</span></span> <span data-ttu-id="a75a1-122">К этим свойствам относятся **Type,** **DefinedSize** и **ActualSize,** **Name** и **NumericScale** и **Precision.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-122">These properties include **Type**, **DefinedSize** and **ActualSize**, **Name**, and **NumericScale** and **Precision**.</span></span>

## <a name="discovering-the-data-type"></a><span data-ttu-id="a75a1-123">Обнаружение типа данных</span><span class="sxs-lookup"><span data-stu-id="a75a1-123">Discovering the data type</span></span>

<span data-ttu-id="a75a1-124">Свойство **Type** указывает тип данных поля.</span><span class="sxs-lookup"><span data-stu-id="a75a1-124">The **Type** property indicates the data type of the field.</span></span> <span data-ttu-id="a75a1-125">Описанные константы типа данных, поддерживаемые ADO, описаны в [DataTypeEnum](datatypeenum.md) в ссылке *программиста ADO.*</span><span class="sxs-lookup"><span data-stu-id="a75a1-125">The data type enumerated constants that are supported by ADO are described in [DataTypeEnum](datatypeenum.md) in the *ADO programmer's reference*.</span></span>

<span data-ttu-id="a75a1-126">Для числимые типы плавающей точки, такие **как adNumeric,** вы можете получить дополнительные сведения.</span><span class="sxs-lookup"><span data-stu-id="a75a1-126">For floating point numeric types such **adNumeric**, you can obtain more information.</span></span> <span data-ttu-id="a75a1-127">Свойство **NumericScale** указывает, сколько цифр справа от десятичной точки будет использоваться для представления значений **для поля**.</span><span class="sxs-lookup"><span data-stu-id="a75a1-127">The **NumericScale** property indicates how many digits to the right of the decimal point will be used to represent values for the **Field**.</span></span> <span data-ttu-id="a75a1-128">Свойство **Precision** указывает максимальное число цифр, используемых для представления значений **для поля.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-128">The **Precision** property specifies the maximum number of digits used to represent values for the **Field**.</span></span>

## <a name="determining-field-size"></a><span data-ttu-id="a75a1-129">Определение размера поля</span><span class="sxs-lookup"><span data-stu-id="a75a1-129">Determining field size</span></span>

<span data-ttu-id="a75a1-130">Свойство **DefinedSize используется** для определения емкости данных объекта **Field.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-130">Use the **DefinedSize** property to determine the data capacity of a **Field** object.</span></span>

<span data-ttu-id="a75a1-131">Используйте **свойство ActualSize,** чтобы вернуть фактическую длину значения **объекта Field.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-131">Use the **ActualSize** property to return the actual length of a **Field** object's value.</span></span> <span data-ttu-id="a75a1-132">Для всех полей свойство **ActualSize** является только для чтения.</span><span class="sxs-lookup"><span data-stu-id="a75a1-132">For all fields, the **ActualSize** property is read-only.</span></span> <span data-ttu-id="a75a1-133">Если ADO не может определить длину значения **объекта Field,** свойство **ActualSize** возвращает **adUnknown.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-133">If ADO cannot determine the length of the **Field** object's value, the **ActualSize** property returns **adUnknown**.</span></span>

<span data-ttu-id="a75a1-134">Свойства **DefinedSize** и **ActualSize** имеют разные цели.</span><span class="sxs-lookup"><span data-stu-id="a75a1-134">The **DefinedSize** and **ActualSize** properties have different purposes.</span></span> <span data-ttu-id="a75a1-135">Например, рассмотрим объект **Field** с объявленным типом **adVarChar** и **свойством DefinedSize** 50, содержащим один символ.</span><span class="sxs-lookup"><span data-stu-id="a75a1-135">For example, consider a **Field** object with a declared type of **adVarChar** and a **DefinedSize** property value of 50, containing a single character.</span></span> <span data-ttu-id="a75a1-136">Возвращаемая значения **свойства ActualSize** — это длина в bytes одного символа.</span><span class="sxs-lookup"><span data-stu-id="a75a1-136">The **ActualSize** property value it returns is the length in bytes of the single character.</span></span>

## <a name="determining-field-contents"></a><span data-ttu-id="a75a1-137">Определение содержимого поля</span><span class="sxs-lookup"><span data-stu-id="a75a1-137">Determining field contents</span></span>

<span data-ttu-id="a75a1-138">Идентификатор столбца из источника данных представлен **свойством Name** **поля.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-138">The identifier of the column from the data source is represented by the **Name** property of the **Field**.</span></span> <span data-ttu-id="a75a1-139">Свойство **Value** объекта **Field** возвращает или задает фактическое содержимое данных поля.</span><span class="sxs-lookup"><span data-stu-id="a75a1-139">The **Value** property of the **Field** object returns or sets the actual data content of the field.</span></span> <span data-ttu-id="a75a1-140">Это свойство по умолчанию.</span><span class="sxs-lookup"><span data-stu-id="a75a1-140">This is the default property.</span></span>

<span data-ttu-id="a75a1-141">Чтобы изменить данные в поле, установите свойство **Value,** равное новому значению правильного типа.</span><span class="sxs-lookup"><span data-stu-id="a75a1-141">To change the data in a field, set the **Value** property equal to a new value of the correct type.</span></span> <span data-ttu-id="a75a1-142">Тип курсора должен поддерживать обновления для изменения содержимого поля.</span><span class="sxs-lookup"><span data-stu-id="a75a1-142">Your cursor type must support updates to change the contents of a field.</span></span> <span data-ttu-id="a75a1-143">Проверка базы данных здесь не проводится в пакетном режиме, поэтому при вызове **UpdateBatch** в таком случае необходимо проверить ошибки.</span><span class="sxs-lookup"><span data-stu-id="a75a1-143">Database validation is not done here in batch mode, so you will need to check for errors when you call **UpdateBatch** in such a case.</span></span> <span data-ttu-id="a75a1-144">Некоторые поставщики также поддерживают свойства **UnderlyingValue** объекта ADO **Field** и **OriginalValue,** чтобы помочь вам в разрешении конфликтов при попытке выполнения пакетных обновлений.</span><span class="sxs-lookup"><span data-stu-id="a75a1-144">Some providers also support the ADO **Field** object's **UnderlyingValue** and **OriginalValue** properties to assist you with resolving conflicts when you attempt to perform batch updates.</span></span> <span data-ttu-id="a75a1-145">Сведения о том, как разрешить такие конфликты, см. в главе [4. Редактирование данных.](chapter-4-editing-data.md)</span><span class="sxs-lookup"><span data-stu-id="a75a1-145">For details about how to resolve such conflicts, see [Chapter 4: Editing Data](chapter-4-editing-data.md).</span></span>

> [!NOTE]
> <span data-ttu-id="a75a1-146">**Значения поля Recordset** невозможно задать при добавлении новых **полей** к **набору записей.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-146">**Recordset Field** values cannot be set when appending new **Fields** to a **Recordset**.</span></span> <span data-ttu-id="a75a1-147">Вместо этого новые **поля** можно примедить к закрытому **набору записей.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-147">Rather, new **Fields** can be appended to a closed **Recordset**.</span></span> <span data-ttu-id="a75a1-148">Затем **необходимо открыть набор** записей, и только после этого значения могут быть назначены этим **полям.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-148">Then the **Recordset** must be opened, and only then can values be assigned to these **Fields**.</span></span>

## <a name="getting-more-field-information"></a><span data-ttu-id="a75a1-149">Получение дополнительных сведений о полях</span><span class="sxs-lookup"><span data-stu-id="a75a1-149">Getting more field information</span></span>

<span data-ttu-id="a75a1-150">Объекты ADO имеют два типа свойств: встроенный и динамический.</span><span class="sxs-lookup"><span data-stu-id="a75a1-150">ADO objects have two types of properties: built-in and dynamic.</span></span> <span data-ttu-id="a75a1-151">До этого момента обсуждались только встроенные свойства объекта **Field.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-151">To this point, only the built-in properties of the **Field** object have been discussed.</span></span>

<span data-ttu-id="a75a1-152">Встроенные свойства — это свойства, реализованные в ADO и сразу доступные любому новому объекту с помощью синтаксиса.</span><span class="sxs-lookup"><span data-stu-id="a75a1-152">Built-in properties are those properties implemented in ADO and immediately available to any new object, using the syntax.</span></span> <span data-ttu-id="a75a1-153">Они не отображаются как **объекты Свойства** в коллекции **свойств** объекта.</span><span class="sxs-lookup"><span data-stu-id="a75a1-153">They do not appear as **Property** objects in an object's **Properties** collection.</span></span>

<span data-ttu-id="a75a1-154">Динамические свойства определяются поставщиком данных и отображаются в коллекции **Свойств** для соответствующего объекта ADO.</span><span class="sxs-lookup"><span data-stu-id="a75a1-154">Dynamic properties are defined by the underlying data provider, and appear in the **Properties** collection for the appropriate ADO object.</span></span> <span data-ttu-id="a75a1-155">Например, свойство, определенное поставщику, может указывать, поддерживает ли объект **Recordset** транзакции или обновление.</span><span class="sxs-lookup"><span data-stu-id="a75a1-155">For example, a property specific to the provider may indicate if a **Recordset** object supports transactions or updating.</span></span> <span data-ttu-id="a75a1-156">Эти дополнительные свойства будут отображаться в качестве  **объектов Свойства** в коллекции Свойств объекта **Recordset.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-156">These additional properties will appear as **Property** objects in that **Recordset** object's **Properties** collection.</span></span> <span data-ttu-id="a75a1-157">Динамические свойства можно ссылаться только через коллекцию, используя синтаксис MyObject.Properties (0) или MyObject.Properties ("Имя").</span><span class="sxs-lookup"><span data-stu-id="a75a1-157">Dynamic properties can be referenced only through the collection, using the syntax MyObject.Properties(0) or or MyObject.Properties("Name") .</span></span>

<span data-ttu-id="a75a1-158">Вы не можете удалить свойство любого типа.</span><span class="sxs-lookup"><span data-stu-id="a75a1-158">You cannot delete either kind of property.</span></span>

<span data-ttu-id="a75a1-159">**Динамический объект Property** имеет четыре встроенных свойства:</span><span class="sxs-lookup"><span data-stu-id="a75a1-159">A dynamic **Property** object has four built-in properties of its own:</span></span>

- <span data-ttu-id="a75a1-160">Свойство **Name** — это строка, определяемая свойством.</span><span class="sxs-lookup"><span data-stu-id="a75a1-160">The **Name** property is a string that identifies the property.</span></span>

- <span data-ttu-id="a75a1-161">Свойство **Type** — это набор, который указывает тип данных свойства.</span><span class="sxs-lookup"><span data-stu-id="a75a1-161">The **Type** property is an integer that specifies the property data type.</span></span>

- <span data-ttu-id="a75a1-162">Свойство **Value** — это вариант, содержащий параметр свойства.</span><span class="sxs-lookup"><span data-stu-id="a75a1-162">The **Value** property is a variant that contains the property setting.</span></span> <span data-ttu-id="a75a1-163">**Значение** — это свойство по умолчанию для **объекта Property.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-163">**Value** is the default property for a **Property** object.</span></span>

- <span data-ttu-id="a75a1-164">Свойство **Attributes** — это **длинное** значение, которое указывает характеристики свойства, определенного поставщику.</span><span class="sxs-lookup"><span data-stu-id="a75a1-164">The **Attributes** property is a **Long** value that indicates characteristics of the property specific to the provider.</span></span>

<span data-ttu-id="a75a1-165">Коллекция **свойств** для объекта **Field** содержит дополнительные метаданные о поле.</span><span class="sxs-lookup"><span data-stu-id="a75a1-165">The **Properties** collection for the **Field** object contains additional metadata about the field.</span></span> <span data-ttu-id="a75a1-166">Содержимое этой коллекции зависит от поставщика.</span><span class="sxs-lookup"><span data-stu-id="a75a1-166">The contents of this collection vary depending upon the provider.</span></span> <span data-ttu-id="a75a1-167">В следующем примере кода рассматривается коллекция **свойств** образца **Recordset,** представленная в начале этой главы.</span><span class="sxs-lookup"><span data-stu-id="a75a1-167">The following code example examines the **Properties** collection of the sample **Recordset** introduced at the beginning of this chapter.</span></span> <span data-ttu-id="a75a1-168">Сначала он рассматривает содержимое коллекции.</span><span class="sxs-lookup"><span data-stu-id="a75a1-168">It first looks at the contents of the collection.</span></span> <span data-ttu-id="a75a1-169">В этом коде используется [поставщик OLE DB](microsoft-ole-db-provider-for-sql-server.md)для SQL Server, поэтому коллекция **свойств** содержит сведения, соответствующие этому поставщику.</span><span class="sxs-lookup"><span data-stu-id="a75a1-169">This code uses the [OLE DB Provider for SQL Server](microsoft-ole-db-provider-for-sql-server.md), so the **Properties** collection contains information relevant to that provider.</span></span>

```vb 
 
'BeginFieldProps 
 Dim objProp As ADODB.Property 
 
 For intLoop = 0 To (objFields.Count - 1) 
 Debug.Print objFields.Item(intLoop).Name 
 
 For Each objProp In objFields(intLoop).Properties 
 Debug.Print vbTab & objProp.Name & " = " & objProp.Value 
 Next objProp 
 Next intLoop 
'EndFieldProps 
```

## <a name="dealing-with-binary-data"></a><span data-ttu-id="a75a1-170">Работа с двоичными данными</span><span class="sxs-lookup"><span data-stu-id="a75a1-170">Dealing with binary data</span></span>

<span data-ttu-id="a75a1-171">Используйте метод [AppendChunk](appendchunk-method-ado.md) на **объекте Field,** чтобы заполнить его длинными двоичными или характерными данными.</span><span class="sxs-lookup"><span data-stu-id="a75a1-171">Use the [AppendChunk](appendchunk-method-ado.md) method on a **Field** object to fill it with long binary or character data.</span></span> <span data-ttu-id="a75a1-172">В ситуациях, когда память системы ограничена, можно использовать метод **AppendChunk** для обработки длинных значений частями, а не целиком.</span><span class="sxs-lookup"><span data-stu-id="a75a1-172">In situations where system memory is limited, you can use the **AppendChunk** method to manipulate long values in portions rather than in their entirety.</span></span>

<span data-ttu-id="a75a1-173">Если бит **adFldLong** в свойстве **Атрибуты** объекта **Field** задан для **True,** для этого поля можно использовать метод **AppendChunk.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-173">If the **adFldLong** bit in the **Attributes** property of a **Field** object is set to **True**, you can use the **AppendChunk** method for that field.</span></span>

<span data-ttu-id="a75a1-174">Первый вызов **AppendChunk на** объект **Field** записывает данные в поле, переописав все существующие данные.</span><span class="sxs-lookup"><span data-stu-id="a75a1-174">The first **AppendChunk** call on a **Field** object writes data to the field, overwriting any existing data.</span></span> <span data-ttu-id="a75a1-175">Последующие **вызовы AppendChunk** добавляются к существующим данным.</span><span class="sxs-lookup"><span data-stu-id="a75a1-175">Subsequent **AppendChunk** calls add to existing data.</span></span> <span data-ttu-id="a75a1-176">Если вы примыкаете к одному полю, а затем задайте или считывая значение другого поля в текущей записи, ADO предполагает, что вы закончили приложение данных к первому полю.</span><span class="sxs-lookup"><span data-stu-id="a75a1-176">If you are appending data to one field and then you set or read the value of another field in the current record, ADO assumes that you are finished appending data to the first field.</span></span> <span data-ttu-id="a75a1-177">Если снова вызвать метод **AppendChunk** в первом поле, ADO интерпретирует вызов как новую операцию **AppendChunk** и переопределает существующие данные.</span><span class="sxs-lookup"><span data-stu-id="a75a1-177">If you call the **AppendChunk** method on the first field again, ADO interprets the call as a new **AppendChunk** operation and overwrites the existing data.</span></span> <span data-ttu-id="a75a1-178">Доступ к полям в других **объектах Recordset,** которые не являются клонами первого объекта **Recordset,** не будет нарушать операции **AppendChunk.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-178">Accessing fields in other **Recordset** objects that are not clones of the first **Recordset** object will not disrupt **AppendChunk** operations.</span></span>

<span data-ttu-id="a75a1-179">Используйте метод **GetChunk** на **объекте Field,** чтобы получить часть или все его длинные двоичные или данные символов.</span><span class="sxs-lookup"><span data-stu-id="a75a1-179">Use the **GetChunk** method on a **Field** object to retrieve part or all of its long binary or character data.</span></span> <span data-ttu-id="a75a1-180">В ситуациях, когда память системы ограничена, можно использовать метод **GetChunk** для обработки длинных значений частями, а не целиком.</span><span class="sxs-lookup"><span data-stu-id="a75a1-180">In situations where system memory is limited, you can use the **GetChunk** method to manipulate long values in portions, rather than in their entirety.</span></span>

<span data-ttu-id="a75a1-181">Данные, **возвращаемые вызовом GetChunk,** назначены *переменной.*</span><span class="sxs-lookup"><span data-stu-id="a75a1-181">The data that a **GetChunk** call returns is assigned to *variable*.</span></span> <span data-ttu-id="a75a1-182">Если *размер* больше остальных данных, метод **GetChunk** возвращает только оставшиеся данные без переменной заполнения *пустыми* пробелами.</span><span class="sxs-lookup"><span data-stu-id="a75a1-182">If *Size* is greater than the remaining data, the **GetChunk** method returns only the remaining data without padding *variable* with empty spaces.</span></span> <span data-ttu-id="a75a1-183">Если поле пусто, метод **GetChunk** возвращает значение null.</span><span class="sxs-lookup"><span data-stu-id="a75a1-183">If the field is empty, the **GetChunk** method returns a null value.</span></span>

<span data-ttu-id="a75a1-184">Каждый **последующий вызов GetChunk** извлекает данные, начиная с того места, где был отключен предыдущий вызов **GetChunk.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-184">Each subsequent **GetChunk** call retrieves data starting from where the previous **GetChunk** call left off.</span></span> <span data-ttu-id="a75a1-185">Однако при сборе данных из одного поля, а затем задайте или считывайте значение другого поля в текущей записи, ADO предполагает, что вы закончили сбор данных из первого поля.</span><span class="sxs-lookup"><span data-stu-id="a75a1-185">However, if you are retrieving data from one field and then set or read the value of another field in the current record, ADO assumes you have finished retrieving data from the first field.</span></span> <span data-ttu-id="a75a1-186">Если снова вызвать метод **GetChunk** в первом поле, ADO интерпретирует вызов как новую операцию **GetChunk** и начинает чтение с самого начала данных.</span><span class="sxs-lookup"><span data-stu-id="a75a1-186">If you call the **GetChunk** method on the first field again, ADO interprets the call as a new **GetChunk** operation and starts reading from the beginning of the data.</span></span> <span data-ttu-id="a75a1-187">Доступ к полям в других **объектах Recordset,** которые не являются клонами первого объекта **Recordset,** не будет нарушать **операции GetChunk.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-187">Accessing fields in other **Recordset** objects that are not clones of the first **Recordset** object will not disrupt **GetChunk** operations.</span></span>

<span data-ttu-id="a75a1-188">Если бит **adFldLong** в свойстве **Атрибуты** объекта **Field** настроен на **True,** для этого поля можно использовать метод **GetChunk.**</span><span class="sxs-lookup"><span data-stu-id="a75a1-188">If the **adFldLong** bit in the **Attributes** property of a **Field** object is set to **True**, you can use the **GetChunk** method for that field.</span></span>

<span data-ttu-id="a75a1-189">Если нет текущей записи при использовании метода **GetChunk** или **AppendChunk** на объекте **Field,** возникает ошибка 3021 (без текущей записи).</span><span class="sxs-lookup"><span data-stu-id="a75a1-189">If there is no current record when you use the **GetChunk** or **AppendChunk** method on a **Field** object, error 3021 (no current record) occurs.</span></span>

<span data-ttu-id="a75a1-190">Примеры использования этих методов для управления двоичными данными см. в примере [метода AppendChunk](appendchunk-method-ado.md) и [метода GetChunk](getchunk-method-ado.md) в ссылке на *программиста ADO.*</span><span class="sxs-lookup"><span data-stu-id="a75a1-190">For an example of using these methods to manipulate binary data, see the [AppendChunk Method](appendchunk-method-ado.md) and [GetChunk Method](getchunk-method-ado.md) examples in the *ADO programmer's reference*.</span></span>

