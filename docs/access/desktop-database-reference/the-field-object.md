---
title: Объект Field (справочник по базе данных access для настольных ПК)
TOCTitle: The Field object
ms:assetid: 55531e04-d74f-6394-df64-1660e5d572ca
ms:mtpsurl: https://msdn.microsoft.com/library/JJ249284(v=office.15)
ms:contentKeyID: 48544926
ms.date: 09/18/2015
mtps_version: v=office.15
localization_priority: Normal
ms.openlocfilehash: 2cbd5752399e5a14f08b7eb944e3a028ba53f561
ms.sourcegitcommit: 8fe462c32b91c87911942c188f3445e85a54137c
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/23/2019
ms.locfileid: "32314023"
---
# <a name="field-object"></a><span data-ttu-id="cfccf-102">Объект Field</span><span class="sxs-lookup"><span data-stu-id="cfccf-102">Field object</span></span>

<span data-ttu-id="cfccf-103">**Область применения**: Access 2013, Office 2013</span><span class="sxs-lookup"><span data-stu-id="cfccf-103">**Applies to**: Access 2013, Office 2013</span></span>

<span data-ttu-id="cfccf-104">Каждый **объект Field** обычно соответствует столбцу в таблице базы данных.</span><span class="sxs-lookup"><span data-stu-id="cfccf-104">Each **Field** object usually corresponds to a column in a database table.</span></span> <span data-ttu-id="cfccf-105">Однако поле **также может** представлять указатель на другой набор **записей,** называемый главой.</span><span class="sxs-lookup"><span data-stu-id="cfccf-105">However, a **Field** can also represent a pointer to another **Recordset**, called a chapter.</span></span> <span data-ttu-id="cfccf-106">Исключения, такие как столбцы глав, будут освещаться далее в этом руководстве.</span><span class="sxs-lookup"><span data-stu-id="cfccf-106">Exceptions, such as chapter columns, will be covered later in this guide.</span></span>

<span data-ttu-id="cfccf-107">Используйте свойство **Value** объектов **Field,** чтобы установить или вернуть данные для текущей записи.</span><span class="sxs-lookup"><span data-stu-id="cfccf-107">Use the **Value** property of **Field** objects to set or return data for the current record.</span></span> <span data-ttu-id="cfccf-108">В зависимости от функциональных возможностей, доступных поставщиком, некоторые коллекции, методы или свойства объекта **Field** могут быть недоступны.</span><span class="sxs-lookup"><span data-stu-id="cfccf-108">Depending on the functionality the provider exposes, some collections, methods, or properties of a **Field** object may not be available.</span></span>

<span data-ttu-id="cfccf-109">С помощью коллекций, методов и свойств объекта **Field** можно сделать следующее:</span><span class="sxs-lookup"><span data-stu-id="cfccf-109">With the collections, methods, and properties of a **Field** object, you can do the following:</span></span>

- <span data-ttu-id="cfccf-110">Возвращает имя поля с помощью свойства **Name.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-110">Return the name of a field by using the **Name** property.</span></span>

- <span data-ttu-id="cfccf-111">Просмотр или изменение данных в поле с помощью свойства **Value.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-111">View or change the data in the field by using the **Value** property.</span></span> <span data-ttu-id="cfccf-112">**Значение** является свойством по умолчанию объекта **Field.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-112">**Value** is the default property of the **Field** object.</span></span>

- <span data-ttu-id="cfccf-113">Возврат основных характеристик поля с помощью свойств **Type,** **Precision** и **NumericScale.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-113">Return the basic characteristics of a field by using the **Type**, **Precision**, and **NumericScale** properties.</span></span>

- <span data-ttu-id="cfccf-114">Возвращает объявленный размер поля с помощью свойства **DefinedSize.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-114">Return the declared size of a field by using the **DefinedSize** property.</span></span>

- <span data-ttu-id="cfccf-115">Возвращает фактический размер данных в заданное поле с помощью **свойства ActualSize.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-115">Return the actual size of the data in a given field by using the **ActualSize** property.</span></span>

- <span data-ttu-id="cfccf-116">Определите, какие типы функций поддерживаются для данного поля, с помощью свойства **Attributes** и **коллекции свойств.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-116">Determine what types of functionality are supported for a given field by using the **Attributes** property and **Properties** collection.</span></span>

- <span data-ttu-id="cfccf-117">Манипулировать значениями полей, содержащих длинные двоичные или длинные данные символов, с помощью методов **AppendChunk** и **GetChunk.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-117">Manipulate the values of fields containing long binary or long character data by using the **AppendChunk** and **GetChunk** methods.</span></span>

- <span data-ttu-id="cfccf-118">Разрешать несоответствия в значениях полей во время пакетного обновления с помощью свойств **OriginalValue** и **UnderlyingValue,** если поставщик поддерживает пакетные обновления.</span><span class="sxs-lookup"><span data-stu-id="cfccf-118">Resolve discrepancies in field values during batch updating by using the **OriginalValue** and **UnderlyingValue** properties, if the provider supports batch updates.</span></span>

## <a name="describing-a-field"></a><span data-ttu-id="cfccf-119">Описание поля</span><span class="sxs-lookup"><span data-stu-id="cfccf-119">Describing a field</span></span>

<span data-ttu-id="cfccf-120">В последующих подраздах будут рассмотрены свойства объекта [Field,](field-object-ado.md) которые представляют сведения, описывая сам объект **Field,** то есть метаданные о поле.</span><span class="sxs-lookup"><span data-stu-id="cfccf-120">The topics that follow will discuss properties of the [Field](field-object-ado.md) object that represent information that describes the **Field** object itself — that is, metadata about the field.</span></span> <span data-ttu-id="cfccf-121">Эти сведения можно использовать для определения значительной части схемы **наборов записей.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-121">This information can be used to determine much about the schema of the **Recordset**.</span></span> <span data-ttu-id="cfccf-122">К этим свойствам относятся **Type,** **DefinedSize** и **ActualSize,** **Name** и **NumericScale** и **Precision.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-122">These properties include **Type**, **DefinedSize** and **ActualSize**, **Name**, and **NumericScale** and **Precision**.</span></span>

## <a name="discovering-the-data-type"></a><span data-ttu-id="cfccf-123">Обнаружение типа данных</span><span class="sxs-lookup"><span data-stu-id="cfccf-123">Discovering the data type</span></span>

<span data-ttu-id="cfccf-124">Свойство **Type** указывает тип данных поля.</span><span class="sxs-lookup"><span data-stu-id="cfccf-124">The **Type** property indicates the data type of the field.</span></span> <span data-ttu-id="cfccf-125">The data type enumerated constants that are supported by ADO are described in [DataTypeEnum](datatypeenum.md) in the *ADO programmer's reference*.</span><span class="sxs-lookup"><span data-stu-id="cfccf-125">The data type enumerated constants that are supported by ADO are described in [DataTypeEnum](datatypeenum.md) in the *ADO programmer's reference*.</span></span>

<span data-ttu-id="cfccf-126">Дополнительные сведения о числовом типе с плавающей за точкой, таких как **adNumeric,** можно получить.</span><span class="sxs-lookup"><span data-stu-id="cfccf-126">For floating point numeric types such **adNumeric**, you can obtain more information.</span></span> <span data-ttu-id="cfccf-127">Свойство **NumericScale** указывает, сколько цифр справа от десятичной точки будет использоваться для представления значений **поля.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-127">The **NumericScale** property indicates how many digits to the right of the decimal point will be used to represent values for the **Field**.</span></span> <span data-ttu-id="cfccf-128">Свойство **Precision** указывает максимальное число цифр, используемых для представления значений **поля.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-128">The **Precision** property specifies the maximum number of digits used to represent values for the **Field**.</span></span>

## <a name="determining-field-size"></a><span data-ttu-id="cfccf-129">Определение размера поля</span><span class="sxs-lookup"><span data-stu-id="cfccf-129">Determining field size</span></span>

<span data-ttu-id="cfccf-130">Используйте свойство **DefinedSize** для определения емкости данных объекта **Field.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-130">Use the **DefinedSize** property to determine the data capacity of a **Field** object.</span></span>

<span data-ttu-id="cfccf-131">Используйте свойство **ActualSize** для возврата фактической длины значения объекта **Field.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-131">Use the **ActualSize** property to return the actual length of a **Field** object's value.</span></span> <span data-ttu-id="cfccf-132">Для всех полей свойство **ActualSize** имеет только чтение.</span><span class="sxs-lookup"><span data-stu-id="cfccf-132">For all fields, the **ActualSize** property is read-only.</span></span> <span data-ttu-id="cfccf-133">Если ADO не удается определить длину значения **объекта Field,** свойство **ActualSize** возвращает **adUnknown**.</span><span class="sxs-lookup"><span data-stu-id="cfccf-133">If ADO cannot determine the length of the **Field** object's value, the **ActualSize** property returns **adUnknown**.</span></span>

<span data-ttu-id="cfccf-134">Свойства **DefinedSize** и **ActualSize** имеют разные цели.</span><span class="sxs-lookup"><span data-stu-id="cfccf-134">The **DefinedSize** and **ActualSize** properties have different purposes.</span></span> <span data-ttu-id="cfccf-135">Например, рассмотрим объект **Field** с объявленным типом **adVarChar** и значением свойства **DefinedSize** 50, содержащим один символ.</span><span class="sxs-lookup"><span data-stu-id="cfccf-135">For example, consider a **Field** object with a declared type of **adVarChar** and a **DefinedSize** property value of 50, containing a single character.</span></span> <span data-ttu-id="cfccf-136">Возвращаемая величина свойства **ActualSize** — это длина в ветвях одного символа.</span><span class="sxs-lookup"><span data-stu-id="cfccf-136">The **ActualSize** property value it returns is the length in bytes of the single character.</span></span>

## <a name="determining-field-contents"></a><span data-ttu-id="cfccf-137">Определение содержимого поля</span><span class="sxs-lookup"><span data-stu-id="cfccf-137">Determining field contents</span></span>

<span data-ttu-id="cfccf-138">Идентификатор столбца из источника данных представлен свойством **Name** **поля.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-138">The identifier of the column from the data source is represented by the **Name** property of the **Field**.</span></span> <span data-ttu-id="cfccf-139">Свойство **Value** объекта **Field** возвращает или задает фактическое содержимое данных поля.</span><span class="sxs-lookup"><span data-stu-id="cfccf-139">The **Value** property of the **Field** object returns or sets the actual data content of the field.</span></span> <span data-ttu-id="cfccf-140">Это свойство по умолчанию.</span><span class="sxs-lookup"><span data-stu-id="cfccf-140">This is the default property.</span></span>

<span data-ttu-id="cfccf-141">Чтобы изменить данные в поле, установите для свойства **Value** значение нового значения правильного типа.</span><span class="sxs-lookup"><span data-stu-id="cfccf-141">To change the data in a field, set the **Value** property equal to a new value of the correct type.</span></span> <span data-ttu-id="cfccf-142">Тип курсора должен поддерживать обновления для изменения содержимого поля.</span><span class="sxs-lookup"><span data-stu-id="cfccf-142">Your cursor type must support updates to change the contents of a field.</span></span> <span data-ttu-id="cfccf-143">Проверка базы данных в этом режиме не проводится в пакетном режиме, поэтому при вызове **UpdateBatch** в таком случае вам потребуется проверить ошибки.</span><span class="sxs-lookup"><span data-stu-id="cfccf-143">Database validation is not done here in batch mode, so you will need to check for errors when you call **UpdateBatch** in such a case.</span></span> <span data-ttu-id="cfccf-144">Некоторые поставщики также поддерживают свойства **UnderlyingValue** и **OriginalValue** объекта Поля **ADO,** чтобы помочь в устранении конфликтов при попытке выполнить пакетное обновление.</span><span class="sxs-lookup"><span data-stu-id="cfccf-144">Some providers also support the ADO **Field** object's **UnderlyingValue** and **OriginalValue** properties to assist you with resolving conflicts when you attempt to perform batch updates.</span></span> <span data-ttu-id="cfccf-145">Сведения о том, как устранить такие конфликты, см. в главе [4 "Изменение данных".](chapter-4-editing-data.md)</span><span class="sxs-lookup"><span data-stu-id="cfccf-145">For details about how to resolve such conflicts, see [Chapter 4: Editing Data](chapter-4-editing-data.md).</span></span>

> [!NOTE]
> <span data-ttu-id="cfccf-146">**Значения поля набора** записей нельзя установить при добавлении новых полей **в** **набор записей.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-146">**Recordset Field** values cannot be set when appending new **Fields** to a **Recordset**.</span></span> <span data-ttu-id="cfccf-147">Вместо этого новые **поля** можно примедить к закрытому **набору записей.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-147">Rather, new **Fields** can be appended to a closed **Recordset**.</span></span> <span data-ttu-id="cfccf-148">Затем необходимо **открыть набор** записей, и только после этого можно будет на назначены значения для этих **полей.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-148">Then the **Recordset** must be opened, and only then can values be assigned to these **Fields**.</span></span>

## <a name="getting-more-field-information"></a><span data-ttu-id="cfccf-149">Получение дополнительных сведений о полях</span><span class="sxs-lookup"><span data-stu-id="cfccf-149">Getting more field information</span></span>

<span data-ttu-id="cfccf-150">У объектов ADO есть два типа свойств: встроенные и динамические.</span><span class="sxs-lookup"><span data-stu-id="cfccf-150">ADO objects have two types of properties: built-in and dynamic.</span></span> <span data-ttu-id="cfccf-151">На данный момент обсуждаются только встроенные свойства объекта **Field.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-151">To this point, only the built-in properties of the **Field** object have been discussed.</span></span>

<span data-ttu-id="cfccf-152">Встроенные свойства — это свойства, реализованные в ADO и сразу же доступные любому новому объекту с использованием синтаксиса.</span><span class="sxs-lookup"><span data-stu-id="cfccf-152">Built-in properties are those properties implemented in ADO and immediately available to any new object, using the syntax.</span></span> <span data-ttu-id="cfccf-153">Они не отображаются как **объекты Property** в коллекции **свойств** объекта.</span><span class="sxs-lookup"><span data-stu-id="cfccf-153">They do not appear as **Property** objects in an object's **Properties** collection.</span></span>

<span data-ttu-id="cfccf-154">Динамические свойства определяются поставщиком данных и отображаются в коллекции **свойств** соответствующего объекта ADO.</span><span class="sxs-lookup"><span data-stu-id="cfccf-154">Dynamic properties are defined by the underlying data provider, and appear in the **Properties** collection for the appropriate ADO object.</span></span> <span data-ttu-id="cfccf-155">Например, свойство, определенное поставщику, может указывать, поддерживает ли объект **Recordset** транзакции или обновление.</span><span class="sxs-lookup"><span data-stu-id="cfccf-155">For example, a property specific to the provider may indicate if a **Recordset** object supports transactions or updating.</span></span> <span data-ttu-id="cfccf-156">Эти дополнительные свойства будут отображаться как **объекты Property** в коллекции **свойств** этого объекта **Recordset.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-156">These additional properties will appear as **Property** objects in that **Recordset** object's **Properties** collection.</span></span> <span data-ttu-id="cfccf-157">На динамические свойства можно ссылаться только через коллекцию, используя синтаксис MyObject.Properties(0) или MyObject.Properties("Name") .</span><span class="sxs-lookup"><span data-stu-id="cfccf-157">Dynamic properties can be referenced only through the collection, using the syntax MyObject.Properties(0) or or MyObject.Properties("Name") .</span></span>

<span data-ttu-id="cfccf-158">Нельзя удалить свойство любого типа.</span><span class="sxs-lookup"><span data-stu-id="cfccf-158">You cannot delete either kind of property.</span></span>

<span data-ttu-id="cfccf-159">**Динамический объект Property** имеет четыре собственных встроенных свойства:</span><span class="sxs-lookup"><span data-stu-id="cfccf-159">A dynamic **Property** object has four built-in properties of its own:</span></span>

- <span data-ttu-id="cfccf-160">Свойство **Name** — это строка, определяемая свойством.</span><span class="sxs-lookup"><span data-stu-id="cfccf-160">The **Name** property is a string that identifies the property.</span></span>

- <span data-ttu-id="cfccf-161">Свойство **Type** — это integer, которое указывает тип данных свойства.</span><span class="sxs-lookup"><span data-stu-id="cfccf-161">The **Type** property is an integer that specifies the property data type.</span></span>

- <span data-ttu-id="cfccf-162">Свойство **Value** — это вариант, содержащий параметр свойства.</span><span class="sxs-lookup"><span data-stu-id="cfccf-162">The **Value** property is a variant that contains the property setting.</span></span> <span data-ttu-id="cfccf-163">**Значение** является свойством по умолчанию для объекта **Property.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-163">**Value** is the default property for a **Property** object.</span></span>

- <span data-ttu-id="cfccf-164">Свойство **Attributes** — **это** длинное значение, которое указывает характеристики свойства, характерного для поставщика.</span><span class="sxs-lookup"><span data-stu-id="cfccf-164">The **Attributes** property is a **Long** value that indicates characteristics of the property specific to the provider.</span></span>

<span data-ttu-id="cfccf-165">Коллекция **Properties** для объекта **Field** содержит дополнительные метаданные о поле.</span><span class="sxs-lookup"><span data-stu-id="cfccf-165">The **Properties** collection for the **Field** object contains additional metadata about the field.</span></span> <span data-ttu-id="cfccf-166">Содержимое этой коллекции зависит от поставщика.</span><span class="sxs-lookup"><span data-stu-id="cfccf-166">The contents of this collection vary depending upon the provider.</span></span> <span data-ttu-id="cfccf-167">В следующем примере кода проверяется коллекция **свойств** образца **объекта Recordset,** представленная в начале этой главы.</span><span class="sxs-lookup"><span data-stu-id="cfccf-167">The following code example examines the **Properties** collection of the sample **Recordset** introduced at the beginning of this chapter.</span></span> <span data-ttu-id="cfccf-168">Сначала он рассматривает содержимое коллекции.</span><span class="sxs-lookup"><span data-stu-id="cfccf-168">It first looks at the contents of the collection.</span></span> <span data-ttu-id="cfccf-169">В этом коде используется [поставщик OLE DB](microsoft-ole-db-provider-for-sql-server.md)для SQL Server, поэтому коллекция **свойств** содержит сведения, релевантные для этого поставщика.</span><span class="sxs-lookup"><span data-stu-id="cfccf-169">This code uses the [OLE DB Provider for SQL Server](microsoft-ole-db-provider-for-sql-server.md), so the **Properties** collection contains information relevant to that provider.</span></span>

```vb 
 
'BeginFieldProps 
 Dim objProp As ADODB.Property 
 
 For intLoop = 0 To (objFields.Count - 1) 
 Debug.Print objFields.Item(intLoop).Name 
 
 For Each objProp In objFields(intLoop).Properties 
 Debug.Print vbTab & objProp.Name & " = " & objProp.Value 
 Next objProp 
 Next intLoop 
'EndFieldProps 
```

## <a name="dealing-with-binary-data"></a><span data-ttu-id="cfccf-170">Работа с двоичными данными</span><span class="sxs-lookup"><span data-stu-id="cfccf-170">Dealing with binary data</span></span>

<span data-ttu-id="cfccf-171">Используйте метод [AppendChunk](appendchunk-method-ado.md) в **объекте Field,** чтобы заполнить его длинными двоичными или символьными данными.</span><span class="sxs-lookup"><span data-stu-id="cfccf-171">Use the [AppendChunk](appendchunk-method-ado.md) method on a **Field** object to fill it with long binary or character data.</span></span> <span data-ttu-id="cfccf-172">В случаях, когда системная память ограничена, можно использовать метод **AppendChunk** для управления длинными значениями по частям, а не полностью.</span><span class="sxs-lookup"><span data-stu-id="cfccf-172">In situations where system memory is limited, you can use the **AppendChunk** method to manipulate long values in portions rather than in their entirety.</span></span>

<span data-ttu-id="cfccf-173">Если бит **adFldLong** в свойстве **Attributes** объекта **Field** имеет свойство **True,** можно использовать метод **AppendChunk** для этого поля.</span><span class="sxs-lookup"><span data-stu-id="cfccf-173">If the **adFldLong** bit in the **Attributes** property of a **Field** object is set to **True**, you can use the **AppendChunk** method for that field.</span></span>

<span data-ttu-id="cfccf-174">Первый вызов **AppendChunk** для объекта **Field** записывает данные в поле, переописав все существующие данные.</span><span class="sxs-lookup"><span data-stu-id="cfccf-174">The first **AppendChunk** call on a **Field** object writes data to the field, overwriting any existing data.</span></span> <span data-ttu-id="cfccf-175">Последующие **вызовы AppendChunk** добавляются к существующим данным.</span><span class="sxs-lookup"><span data-stu-id="cfccf-175">Subsequent **AppendChunk** calls add to existing data.</span></span> <span data-ttu-id="cfccf-176">Если вы при добавлении данных в одно поле и затем установили или считыали значение другого поля в текущей записи, ADO предполагает, что вы завершили добавление данных в первое поле.</span><span class="sxs-lookup"><span data-stu-id="cfccf-176">If you are appending data to one field and then you set or read the value of another field in the current record, ADO assumes that you are finished appending data to the first field.</span></span> <span data-ttu-id="cfccf-177">При повторном вызове метода **AppendChunk** в первом поле ADO интерпретирует вызов как новую операцию **AppendChunk** и переоценит существующие данные.</span><span class="sxs-lookup"><span data-stu-id="cfccf-177">If you call the **AppendChunk** method on the first field again, ADO interprets the call as a new **AppendChunk** operation and overwrites the existing data.</span></span> <span data-ttu-id="cfccf-178">Доступ к полям в других **объектах Recordset,** которые не являются клонами первого объекта **Recordset,** не нарушит операции **AppendChunk.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-178">Accessing fields in other **Recordset** objects that are not clones of the first **Recordset** object will not disrupt **AppendChunk** operations.</span></span>

<span data-ttu-id="cfccf-179">Используйте метод **GetChunk** для объекта **Field,** чтобы получить часть или все длинные двоичные или символьные данные.</span><span class="sxs-lookup"><span data-stu-id="cfccf-179">Use the **GetChunk** method on a **Field** object to retrieve part or all of its long binary or character data.</span></span> <span data-ttu-id="cfccf-180">В ситуациях, когда системная память ограничена, можно использовать метод **GetChunk** для управления длинными значениями по частям, а не полностью.</span><span class="sxs-lookup"><span data-stu-id="cfccf-180">In situations where system memory is limited, you can use the **GetChunk** method to manipulate long values in portions, rather than in their entirety.</span></span>

<span data-ttu-id="cfccf-181">Данные, возвращаемые вызовом **GetChunk,** назначены *переменной.*</span><span class="sxs-lookup"><span data-stu-id="cfccf-181">The data that a **GetChunk** call returns is assigned to *variable*.</span></span> <span data-ttu-id="cfccf-182">Если *размер* больше остальных данных, метод **GetChunk** возвращает только оставшиеся данные без переменной заполнения *с* пустыми пробелами.</span><span class="sxs-lookup"><span data-stu-id="cfccf-182">If *Size* is greater than the remaining data, the **GetChunk** method returns only the remaining data without padding *variable* with empty spaces.</span></span> <span data-ttu-id="cfccf-183">Если поле пустое, метод **GetChunk** возвращает значение null.</span><span class="sxs-lookup"><span data-stu-id="cfccf-183">If the field is empty, the **GetChunk** method returns a null value.</span></span>

<span data-ttu-id="cfccf-184">Каждый **последующий вызов GetChunk** извлекает данные, начиная с того места, где был отключен предыдущий вызов **GetChunk.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-184">Each subsequent **GetChunk** call retrieves data starting from where the previous **GetChunk** call left off.</span></span> <span data-ttu-id="cfccf-185">Однако если вы искомые данные из одного поля, а затем задайте или считайте значение другого поля в текущей записи, ADO предполагает, что вы завершили искомые данные из первого поля.</span><span class="sxs-lookup"><span data-stu-id="cfccf-185">However, if you are retrieving data from one field and then set or read the value of another field in the current record, ADO assumes you have finished retrieving data from the first field.</span></span> <span data-ttu-id="cfccf-186">При повторном вызове метода **GetChunk** в первом поле ADO интерпретирует вызов как новую операцию **GetChunk** и начинает чтение с начала данных.</span><span class="sxs-lookup"><span data-stu-id="cfccf-186">If you call the **GetChunk** method on the first field again, ADO interprets the call as a new **GetChunk** operation and starts reading from the beginning of the data.</span></span> <span data-ttu-id="cfccf-187">Доступ к полям в других **объектах Recordset,** которые не являются клонами первого объекта **Recordset,** не нарушит операции **GetChunk.**</span><span class="sxs-lookup"><span data-stu-id="cfccf-187">Accessing fields in other **Recordset** objects that are not clones of the first **Recordset** object will not disrupt **GetChunk** operations.</span></span>

<span data-ttu-id="cfccf-188">Если бит **adFldLong** в свойстве **Attributes** объекта **Field** имеет свойство **True,** можно использовать метод **GetChunk** для этого поля.</span><span class="sxs-lookup"><span data-stu-id="cfccf-188">If the **adFldLong** bit in the **Attributes** property of a **Field** object is set to **True**, you can use the **GetChunk** method for that field.</span></span>

<span data-ttu-id="cfccf-189">Если при использовании метода **GetChunk** или **AppendChunk** для объекта **Field** нет текущей записи, возникает ошибка 3021 (текущая запись не возникает).</span><span class="sxs-lookup"><span data-stu-id="cfccf-189">If there is no current record when you use the **GetChunk** or **AppendChunk** method on a **Field** object, error 3021 (no current record) occurs.</span></span>

<span data-ttu-id="cfccf-190">Пример использования этих методов для управления двоичными данными см. в примерах [appendChunk Method](appendchunk-method-ado.md) и [GetChunk Method](getchunk-method-ado.md) в справочнике *программиста ADO.*</span><span class="sxs-lookup"><span data-stu-id="cfccf-190">For an example of using these methods to manipulate binary data, see the [AppendChunk Method](appendchunk-method-ado.md) and [GetChunk Method](getchunk-method-ado.md) examples in the *ADO programmer's reference*.</span></span>

