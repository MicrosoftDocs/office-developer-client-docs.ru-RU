---
title: Объект Field (Справочник по базам данных Access на компьютере)
TOCTitle: The Field object
ms:assetid: 55531e04-d74f-6394-df64-1660e5d572ca
ms:mtpsurl: https://msdn.microsoft.com/library/JJ249284(v=office.15)
ms:contentKeyID: 48544926
ms.date: 09/18/2015
mtps_version: v=office.15
localization_priority: Normal
ms.openlocfilehash: 2cbd5752399e5a14f08b7eb944e3a028ba53f561
ms.sourcegitcommit: 8fe462c32b91c87911942c188f3445e85a54137c
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/23/2019
ms.locfileid: "32314023"
---
# <a name="field-object"></a><span data-ttu-id="eaa0b-102">Объект Field</span><span class="sxs-lookup"><span data-stu-id="eaa0b-102">Field object</span></span>

<span data-ttu-id="eaa0b-103">**Область применения**: Access 2013, Office 2013</span><span class="sxs-lookup"><span data-stu-id="eaa0b-103">**Applies to**: Access 2013, Office 2013</span></span>

<span data-ttu-id="eaa0b-104">Каждый объект **field** обычно соответствует столбцу в таблице базы данных.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-104">Each **Field** object usually corresponds to a column in a database table.</span></span> <span data-ttu-id="eaa0b-105">Однако **поле** также может представлять указатель на другой **набор записей**, который называется главой.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-105">However, a **Field** can also represent a pointer to another **Recordset**, called a chapter.</span></span> <span data-ttu-id="eaa0b-106">Исключения, например столбцы глав, будут рассмотрены далее в этом руководстве.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-106">Exceptions, such as chapter columns, will be covered later in this guide.</span></span>

<span data-ttu-id="eaa0b-107">Используйте свойство **value** объектов **field** , чтобы задать или вернуть данные для текущей записи.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-107">Use the **Value** property of **Field** objects to set or return data for the current record.</span></span> <span data-ttu-id="eaa0b-108">В зависимости от функциональных возможностей, предоставляемых поставщиком, некоторые коллекции, методы или свойства объекта **field** могут быть недоступны.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-108">Depending on the functionality the provider exposes, some collections, methods, or properties of a **Field** object may not be available.</span></span>

<span data-ttu-id="eaa0b-109">В коллекциях, методах и свойствах объекта **field** можно выполнить следующие действия:</span><span class="sxs-lookup"><span data-stu-id="eaa0b-109">With the collections, methods, and properties of a **Field** object, you can do the following:</span></span>

- <span data-ttu-id="eaa0b-110">Возврат имени поля с помощью свойства **Name** .</span><span class="sxs-lookup"><span data-stu-id="eaa0b-110">Return the name of a field by using the **Name** property.</span></span>

- <span data-ttu-id="eaa0b-111">Просмотрите или измените данные в поле, используя свойство **value** .</span><span class="sxs-lookup"><span data-stu-id="eaa0b-111">View or change the data in the field by using the **Value** property.</span></span> <span data-ttu-id="eaa0b-112">**Value** является свойством по умолчанию объекта **field** .</span><span class="sxs-lookup"><span data-stu-id="eaa0b-112">**Value** is the default property of the **Field** object.</span></span>

- <span data-ttu-id="eaa0b-113">ВозВращайте основные характеристики поля, используя свойства **Type**, **Precision**и **NumericScale** .</span><span class="sxs-lookup"><span data-stu-id="eaa0b-113">Return the basic characteristics of a field by using the **Type**, **Precision**, and **NumericScale** properties.</span></span>

- <span data-ttu-id="eaa0b-114">Возврат объявленного размера поля с помощью свойства **DefinedSize** .</span><span class="sxs-lookup"><span data-stu-id="eaa0b-114">Return the declared size of a field by using the **DefinedSize** property.</span></span>

- <span data-ttu-id="eaa0b-115">Возвращает фактический размер данных в заданном поле с помощью свойства **ActualSize** .</span><span class="sxs-lookup"><span data-stu-id="eaa0b-115">Return the actual size of the data in a given field by using the **ActualSize** property.</span></span>

- <span data-ttu-id="eaa0b-116">Определите, какие типы функций поддерживаются для данного поля, с помощью свойства **Attributes** и коллекции **свойств** .</span><span class="sxs-lookup"><span data-stu-id="eaa0b-116">Determine what types of functionality are supported for a given field by using the **Attributes** property and **Properties** collection.</span></span>

- <span data-ttu-id="eaa0b-117">Управлять значениями полей с длинными двоичными или длинными символьными данными с помощью методов \*\*\*\* **AppendChunk** и.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-117">Manipulate the values of fields containing long binary or long character data by using the **AppendChunk** and **GetChunk** methods.</span></span>

- <span data-ttu-id="eaa0b-118">РазРешите несоответствия в значениях полей во время пакетного обновления с помощью свойств **originalValue** и **UnderlyingValue** , если поставщик поддерживает пакетное обновление.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-118">Resolve discrepancies in field values during batch updating by using the **OriginalValue** and **UnderlyingValue** properties, if the provider supports batch updates.</span></span>

## <a name="describing-a-field"></a><span data-ttu-id="eaa0b-119">Описание поля</span><span class="sxs-lookup"><span data-stu-id="eaa0b-119">Describing a field</span></span>

<span data-ttu-id="eaa0b-120">В следующих статьях будут обсуждаться свойства объекта [field](field-object-ado.md) , представляющие собой сведения, описывающие объект **field** (то есть, метаданные для поля).</span><span class="sxs-lookup"><span data-stu-id="eaa0b-120">The topics that follow will discuss properties of the [Field](field-object-ado.md) object that represent information that describes the **Field** object itself — that is, metadata about the field.</span></span> <span data-ttu-id="eaa0b-121">Эти сведения можно использовать для определения большого объема схемы **набора записей**.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-121">This information can be used to determine much about the schema of the **Recordset**.</span></span> <span data-ttu-id="eaa0b-122">К этим свойствам относятся **Type**, **DefinedSize** и **ActualSize**, **Name**и **NumericScale** , а также **точность**.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-122">These properties include **Type**, **DefinedSize** and **ActualSize**, **Name**, and **NumericScale** and **Precision**.</span></span>

## <a name="discovering-the-data-type"></a><span data-ttu-id="eaa0b-123">Обнаружение типа данных</span><span class="sxs-lookup"><span data-stu-id="eaa0b-123">Discovering the data type</span></span>

<span data-ttu-id="eaa0b-124">Свойство **Type** указывает тип данных поля.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-124">The **Type** property indicates the data type of the field.</span></span> <span data-ttu-id="eaa0b-125">Перечислимые константы с типом данных, которые поддерживаются ADO, описаны в разделе [DataTypeEnum](datatypeenum.md) в справочнике программиста по *ADO*.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-125">The data type enumerated constants that are supported by ADO are described in [DataTypeEnum](datatypeenum.md) in the *ADO programmer's reference*.</span></span>

<span data-ttu-id="eaa0b-126">Для числовых типов с плавающей запятой, таких как **аднумерик**, вы можете получить дополнительные сведения.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-126">For floating point numeric types such **adNumeric**, you can obtain more information.</span></span> <span data-ttu-id="eaa0b-127">Свойство **NumericScale** указывает, сколько цифр справа от десятичной точки будет использоваться для представления значений для **поля**.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-127">The **NumericScale** property indicates how many digits to the right of the decimal point will be used to represent values for the **Field**.</span></span> <span data-ttu-id="eaa0b-128">Свойство **Precision** указывает максимальное количество цифр, используемых для представления значений для **поля**.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-128">The **Precision** property specifies the maximum number of digits used to represent values for the **Field**.</span></span>

## <a name="determining-field-size"></a><span data-ttu-id="eaa0b-129">Определение размера поля</span><span class="sxs-lookup"><span data-stu-id="eaa0b-129">Determining field size</span></span>

<span data-ttu-id="eaa0b-130">Используйте свойство **DefinedSize** для определения емкости данных объекта **field** .</span><span class="sxs-lookup"><span data-stu-id="eaa0b-130">Use the **DefinedSize** property to determine the data capacity of a **Field** object.</span></span>

<span data-ttu-id="eaa0b-131">Используйте свойство **ActualSize** , чтобы возвратить фактическую длину значения объекта **field** .</span><span class="sxs-lookup"><span data-stu-id="eaa0b-131">Use the **ActualSize** property to return the actual length of a **Field** object's value.</span></span> <span data-ttu-id="eaa0b-132">Для всех полей свойство **ActualSize** доступно только для чтения.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-132">For all fields, the **ActualSize** property is read-only.</span></span> <span data-ttu-id="eaa0b-133">Если ADO не удается определить длину значения объекта **field** , свойство **ActualSize** возвращает **адункновн**.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-133">If ADO cannot determine the length of the **Field** object's value, the **ActualSize** property returns **adUnknown**.</span></span>

<span data-ttu-id="eaa0b-134">Свойства **DefinedSize** и **ActualSize** имеют разные цели.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-134">The **DefinedSize** and **ActualSize** properties have different purposes.</span></span> <span data-ttu-id="eaa0b-135">Например, рассмотрим объект **field** с объявляемым типом **адварчар** и значением свойства **DefinedSize** , равным 50, с одним символом.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-135">For example, consider a **Field** object with a declared type of **adVarChar** and a **DefinedSize** property value of 50, containing a single character.</span></span> <span data-ttu-id="eaa0b-136">Возвращаемое значение свойства **ActualSize** — это длина в байтах одного символа.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-136">The **ActualSize** property value it returns is the length in bytes of the single character.</span></span>

## <a name="determining-field-contents"></a><span data-ttu-id="eaa0b-137">Определение содержимого поля</span><span class="sxs-lookup"><span data-stu-id="eaa0b-137">Determining field contents</span></span>

<span data-ttu-id="eaa0b-138">Идентификатор столбца из источника данных представлен свойством **Name** **поля**.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-138">The identifier of the column from the data source is represented by the **Name** property of the **Field**.</span></span> <span data-ttu-id="eaa0b-139">Свойство **value** объекта **field** Возвращает или задает фактическое содержимое данных поля.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-139">The **Value** property of the **Field** object returns or sets the actual data content of the field.</span></span> <span data-ttu-id="eaa0b-140">Это свойство по умолчанию.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-140">This is the default property.</span></span>

<span data-ttu-id="eaa0b-141">Чтобы изменить данные в поле, присвойте свойству **value значение** , равное новому значению соответствующего типа.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-141">To change the data in a field, set the **Value** property equal to a new value of the correct type.</span></span> <span data-ttu-id="eaa0b-142">Тип курсора должен поддерживать обновления для изменения содержимого поля.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-142">Your cursor type must support updates to change the contents of a field.</span></span> <span data-ttu-id="eaa0b-143">Проверка базы данных не выполняется в пакетном режиме, поэтому при вызове **UpdateBatch** в таком случае необходимо будет проверять наличие ошибок.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-143">Database validation is not done here in batch mode, so you will need to check for errors when you call **UpdateBatch** in such a case.</span></span> <span data-ttu-id="eaa0b-144">Некоторые поставщики также поддерживают свойства **UnderlyingValue** и **ORIGINALVALUE** объекта **поля** ADO, помогающие устранять конфликты при попытке выполнить пакетное обновление.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-144">Some providers also support the ADO **Field** object's **UnderlyingValue** and **OriginalValue** properties to assist you with resolving conflicts when you attempt to perform batch updates.</span></span> <span data-ttu-id="eaa0b-145">Дополнительные сведения о разрешении таких конфликтов приведены в [главе 4: Редактирование данных](chapter-4-editing-data.md).</span><span class="sxs-lookup"><span data-stu-id="eaa0b-145">For details about how to resolve such conflicts, see [Chapter 4: Editing Data](chapter-4-editing-data.md).</span></span>

> [!NOTE]
> <span data-ttu-id="eaa0b-146">Значения **полей набора записей** не могут быть заданы при добавлении новых **полей** в **набор записей**.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-146">**Recordset Field** values cannot be set when appending new **Fields** to a **Recordset**.</span></span> <span data-ttu-id="eaa0b-147">Вместо этого новые **поля** можно добавлять в закрытый **набор записей**.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-147">Rather, new **Fields** can be appended to a closed **Recordset**.</span></span> <span data-ttu-id="eaa0b-148">Затем должен быть открыт **набор записей** , а этим **полям**могут быть назначены только значения.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-148">Then the **Recordset** must be opened, and only then can values be assigned to these **Fields**.</span></span>

## <a name="getting-more-field-information"></a><span data-ttu-id="eaa0b-149">Извлечение дополнительных сведений о полях</span><span class="sxs-lookup"><span data-stu-id="eaa0b-149">Getting more field information</span></span>

<span data-ttu-id="eaa0b-150">Объекты ADO имеют два типа свойств: встроенные и динамические.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-150">ADO objects have two types of properties: built-in and dynamic.</span></span> <span data-ttu-id="eaa0b-151">К этому моменту были рассмотрены только встроенные свойства объекта **field** .</span><span class="sxs-lookup"><span data-stu-id="eaa0b-151">To this point, only the built-in properties of the **Field** object have been discussed.</span></span>

<span data-ttu-id="eaa0b-152">Встроенные свойства — это свойства, реализованные в ADO и сразу доступные для любого нового объекта с помощью синтаксиса.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-152">Built-in properties are those properties implemented in ADO and immediately available to any new object, using the syntax.</span></span> <span data-ttu-id="eaa0b-153">Они не отображаются как объекты **свойств** в коллекции **свойств** объекта.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-153">They do not appear as **Property** objects in an object's **Properties** collection.</span></span>

<span data-ttu-id="eaa0b-154">Динамические свойства определяются базовым поставщиком данных и отображаются в коллекции **свойств** для соответствующего объекта ADO.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-154">Dynamic properties are defined by the underlying data provider, and appear in the **Properties** collection for the appropriate ADO object.</span></span> <span data-ttu-id="eaa0b-155">Например, свойство, относящееся к поставщику, может указывать на то, поддерживает ли объект **Recordset** транзакции или обновление.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-155">For example, a property specific to the provider may indicate if a **Recordset** object supports transactions or updating.</span></span> <span data-ttu-id="eaa0b-156">Эти дополнительные свойства будут отображаться в виде объектов **Property** в коллекции **свойств** объекта **Recordset** .</span><span class="sxs-lookup"><span data-stu-id="eaa0b-156">These additional properties will appear as **Property** objects in that **Recordset** object's **Properties** collection.</span></span> <span data-ttu-id="eaa0b-157">На динамические свойства можно ссылаться только через коллекцию, используя синтаксис MyObject. Properties (0) или или MyObject. Properties ("Name").</span><span class="sxs-lookup"><span data-stu-id="eaa0b-157">Dynamic properties can be referenced only through the collection, using the syntax MyObject.Properties(0) or or MyObject.Properties("Name") .</span></span>

<span data-ttu-id="eaa0b-158">Вы не можете удалить любой тип свойства.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-158">You cannot delete either kind of property.</span></span>

<span data-ttu-id="eaa0b-159">Объект динамического **Свойства** имеет четыре встроенных свойства:</span><span class="sxs-lookup"><span data-stu-id="eaa0b-159">A dynamic **Property** object has four built-in properties of its own:</span></span>

- <span data-ttu-id="eaa0b-160">Свойство **Name** — это строка, идентифицирующая свойство.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-160">The **Name** property is a string that identifies the property.</span></span>

- <span data-ttu-id="eaa0b-161">Свойство **Type** — это целое число, определяющее тип данных свойства.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-161">The **Type** property is an integer that specifies the property data type.</span></span>

- <span data-ttu-id="eaa0b-162">Свойство **value** является вариантом, который содержит параметр свойства.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-162">The **Value** property is a variant that contains the property setting.</span></span> <span data-ttu-id="eaa0b-163">**Value** является свойством по умолчанию для объекта **Property** .</span><span class="sxs-lookup"><span data-stu-id="eaa0b-163">**Value** is the default property for a **Property** object.</span></span>

- <span data-ttu-id="eaa0b-164">Свойство **Attributes** — это **длинное** значение, которое указывает характеристики свойства, характерного для поставщика.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-164">The **Attributes** property is a **Long** value that indicates characteristics of the property specific to the provider.</span></span>

<span data-ttu-id="eaa0b-165">Коллекция **Properties** для объекта **field** содержит дополнительные метаданные о поле.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-165">The **Properties** collection for the **Field** object contains additional metadata about the field.</span></span> <span data-ttu-id="eaa0b-166">Содержимое этой коллекции зависит от поставщика.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-166">The contents of this collection vary depending upon the provider.</span></span> <span data-ttu-id="eaa0b-167">В приведенном ниже примере кода показана проверка коллекции **свойств** примера объекта **Recordset** , представленного в начале этой главы.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-167">The following code example examines the **Properties** collection of the sample **Recordset** introduced at the beginning of this chapter.</span></span> <span data-ttu-id="eaa0b-168">Сначала он просматривает содержимое коллекции.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-168">It first looks at the contents of the collection.</span></span> <span data-ttu-id="eaa0b-169">В этом коде используется [поставщик OLE DB для SQL Server](microsoft-ole-db-provider-for-sql-server.md), поэтому коллекция **Properties** содержит сведения, относящиеся к этому поставщику.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-169">This code uses the [OLE DB Provider for SQL Server](microsoft-ole-db-provider-for-sql-server.md), so the **Properties** collection contains information relevant to that provider.</span></span>

```vb 
 
'BeginFieldProps 
 Dim objProp As ADODB.Property 
 
 For intLoop = 0 To (objFields.Count - 1) 
 Debug.Print objFields.Item(intLoop).Name 
 
 For Each objProp In objFields(intLoop).Properties 
 Debug.Print vbTab & objProp.Name & " = " & objProp.Value 
 Next objProp 
 Next intLoop 
'EndFieldProps 
```

## <a name="dealing-with-binary-data"></a><span data-ttu-id="eaa0b-170">Работа с двоичными данными</span><span class="sxs-lookup"><span data-stu-id="eaa0b-170">Dealing with binary data</span></span>

<span data-ttu-id="eaa0b-171">Используйте метод [AppendChunk](appendchunk-method-ado.md) для объекта **field** , чтобы заполнить его длинными двоичными или символьными данными.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-171">Use the [AppendChunk](appendchunk-method-ado.md) method on a **Field** object to fill it with long binary or character data.</span></span> <span data-ttu-id="eaa0b-172">В ситуациях, когда ограничена системная память, можно использовать метод **AppendChunk** для управления длинными значениями в частях, а не целиком.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-172">In situations where system memory is limited, you can use the **AppendChunk** method to manipulate long values in portions rather than in their entirety.</span></span>

<span data-ttu-id="eaa0b-173">Если для бита **адфлдлонг** в свойстве **Attribute** объекта **field** задано **значение true**, можно использовать метод **AppendChunk** для этого поля.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-173">If the **adFldLong** bit in the **Attributes** property of a **Field** object is set to **True**, you can use the **AppendChunk** method for that field.</span></span>

<span data-ttu-id="eaa0b-174">Первый вызов **AppendChunk** для объекта **field** записывает данные в поле, перезаписывая существующие данные.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-174">The first **AppendChunk** call on a **Field** object writes data to the field, overwriting any existing data.</span></span> <span data-ttu-id="eaa0b-175">Последующие вызовы **AppendChunk** добавляются к существующим данным.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-175">Subsequent **AppendChunk** calls add to existing data.</span></span> <span data-ttu-id="eaa0b-176">Если вы добавляете данные в одно поле, а затем задаете или просматриваете значение другого поля в текущей записи, ADO считает, что вы завершили добавление данных в первое поле.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-176">If you are appending data to one field and then you set or read the value of another field in the current record, ADO assumes that you are finished appending data to the first field.</span></span> <span data-ttu-id="eaa0b-177">Если вызвать метод **AppendChunk** для первого поля еще раз, ADO интерпретирует вызов как новую операцию **AppendChunk** и перезаписывает существующие данные.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-177">If you call the **AppendChunk** method on the first field again, ADO interprets the call as a new **AppendChunk** operation and overwrites the existing data.</span></span> <span data-ttu-id="eaa0b-178">Доступ к полям в других объектах **Recordset** , которые не являются клонами первого объекта **Recordset** , не приводит к нарушению операций **AppendChunk** .</span><span class="sxs-lookup"><span data-stu-id="eaa0b-178">Accessing fields in other **Recordset** objects that are not clones of the first **Recordset** object will not disrupt **AppendChunk** operations.</span></span>

<span data-ttu-id="eaa0b-179">Используйте метод \*\*\*\* GetObject для объекта **field** , чтобы извлечь часть или все его длинные двоичные или символьные данные.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-179">Use the **GetChunk** method on a **Field** object to retrieve part or all of its long binary or character data.</span></span> <span data-ttu-id="eaa0b-180">В ситуациях, когда ограничена системная память, можно использовать метод методомического **блока** для управления длинными значениями в частях, а не целиком.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-180">In situations where system memory is limited, you can use the **GetChunk** method to manipulate long values in portions, rather than in their entirety.</span></span>

<span data-ttu-id="eaa0b-181">Данные, возвращаемые вызовом функции- **блока** , назначаются *переменной*.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-181">The data that a **GetChunk** call returns is assigned to *variable*.</span></span> <span data-ttu-id="eaa0b-182">Если *Размер* больше остальных данных, метод IsEmpty возвращает только \*\*\*\* оставшиеся данные без *переменной* заполнения с пустыми пробелами.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-182">If *Size* is greater than the remaining data, the **GetChunk** method returns only the remaining data without padding *variable* with empty spaces.</span></span> <span data-ttu-id="eaa0b-183">Если поле не заполнено, метод **блока** данных возвращает значение null.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-183">If the field is empty, the **GetChunk** method returns a null value.</span></span>

<span data-ttu-id="eaa0b-184">Каждый последующий вызов метода возвращает данные, начиная с того места, где предыдущий вызов **блока** . \*\*\*\*</span><span class="sxs-lookup"><span data-stu-id="eaa0b-184">Each subsequent **GetChunk** call retrieves data starting from where the previous **GetChunk** call left off.</span></span> <span data-ttu-id="eaa0b-185">Однако при получении данных из одного поля и последующем установке или считывании значения другого поля в текущей записи, ADO предполагается, что вы завершили извлечение данных из первого поля.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-185">However, if you are retrieving data from one field and then set or read the value of another field in the current record, ADO assumes you have finished retrieving data from the first field.</span></span> <span data-ttu-id="eaa0b-186">При повторном вызове метода переФрагмента для первого поля, ADO интерпретирует вызов как новую операцию, а \*\*\*\* затем начинает чтение с начала данных. \*\*\*\*</span><span class="sxs-lookup"><span data-stu-id="eaa0b-186">If you call the **GetChunk** method on the first field again, ADO interprets the call as a new **GetChunk** operation and starts reading from the beginning of the data.</span></span> <span data-ttu-id="eaa0b-187">Доступ к полям в других объектах **Recordset** , которые не являются клонами первого объекта **Recordset** , не приводит к нарушению операций с **блоком** .</span><span class="sxs-lookup"><span data-stu-id="eaa0b-187">Accessing fields in other **Recordset** objects that are not clones of the first **Recordset** object will not disrupt **GetChunk** operations.</span></span>

<span data-ttu-id="eaa0b-188">Если для бита **адфлдлонг** в свойстве **Attribute** объекта **field** задано **значение true**, можно использовать метод GetObject для этого \*\*\*\* поля.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-188">If the **adFldLong** bit in the **Attributes** property of a **Field** object is set to **True**, you can use the **GetChunk** method for that field.</span></span>

<span data-ttu-id="eaa0b-189">Если при использовании метода GetObject или **AppendChunk** для объекта **field** отсутствует текущая запись, возникает ошибка 3021 (нет текущей записи). \*\*\*\*</span><span class="sxs-lookup"><span data-stu-id="eaa0b-189">If there is no current record when you use the **GetChunk** or **AppendChunk** method on a **Field** object, error 3021 (no current record) occurs.</span></span>

<span data-ttu-id="eaa0b-190">Пример использования этих методов для работы с двоичными данными приведен в примерах [методов](appendchunk-method-ado.md) и методов [](getchunk-method-ado.md) AppendChunk в справочнике программиста по *ADO*.</span><span class="sxs-lookup"><span data-stu-id="eaa0b-190">For an example of using these methods to manipulate binary data, see the [AppendChunk Method](appendchunk-method-ado.md) and [GetChunk Method](getchunk-method-ado.md) examples in the *ADO programmer's reference*.</span></span>

