---
title: Макрокоманда ImportExportText
TOCTitle: ImportExportText macro action
ms:assetid: 366fa095-6f09-7c22-e734-bfa585cfe79e
ms:mtpsurl: https://msdn.microsoft.com/library/Ff192475(v=office.15)
ms:contentKeyID: 48544171
ms.date: 09/18/2015
mtps_version: v=office.15
f1_keywords:
- vbaac10.chm168097
f1_categories:
- Office.Version=v15
ms.openlocfilehash: be5497b7f1dbed4d32f25f2a675c3f996ed47c9f
ms.sourcegitcommit: d7248f803002b31cf7fc561b03530199a9b0a8fd
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 11/02/2018
ms.locfileid: "25925452"
---
# <a name="importexporttext-macro-action"></a>Макрокоманда ImportExportText

**Применимо к**: Access 2013, Office 2013

Действие **ImportExportText** можно использовать для импорта или экспорта текста между текущей базы данных Microsoft Access (MDB- или .accdb) или проект Microsoft Access (файлы с расширением ADP) и в текстовый файл. Также можно связать данные в текстовый файл текущей базы данных Access. С помощью связанного текстового файла можно просмотреть текстовых данных с доступом по-прежнему предоставляя полный доступ к данным из программы обработки текста. Также импортировать из, экспортировать и ссылка на таблицы или списка в файле HTML (\*.html).

> [!NOTE]
> При связывании данных в текстовый файл или HTML-файл данных доступен только для чтения в клиенте.
> 
> Это действие не разрешено, если база данных не является доверенной. 

## <a name="setting"></a>Параметр

Действие **ImportExportText** состоит из следующих аргументов.

<table>
<colgroup>
<col style="width: 50%" />
<col style="width: 50%" />
</colgroup>
<thead>
<tr class="header">
<th><p>Аргумент макрокоманды</p></th>
<th><p>Описание</p></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><p><strong>Тип передачи</strong></p></td>
<td><p>Тип переноса, который требуется сделать. Импорт данных из Экспорт данных в или ссылка на данные в файлы с разделителями или фиксированной длины текста или HTML-файлы. Также можно экспортировать данные в файл данных слияния почты Microsoft Word, которое затем используется с функцией слияния Word для создания объединенных документов и других наклейки. Выберите <strong>запятые импорта</strong>, <strong>импорта Фиксированная ширина</strong>, <strong>Импорт HTML</strong>, <strong>Экспорт с разделителями</strong>, <strong>экспортировать Фиксированная ширина</strong>, <strong>Экспорт HTML</strong>, <strong>Экспорт Word для объединения Windows</strong>, <strong>запятые ссылок</strong>, <strong>ссылку Фиксированная ширина </strong>, или <strong>Ссылку HTML</strong> в поле <strong>Тип преобразования</strong> в разделе <strong>Действие аргументы</strong> в области построения макросов. Значение по умолчанию — <strong>Запятые импорта</strong>.</p>

> [!NOTE]
> <P>Только <STRONG>Запятые импорта</STRONG>, <STRONG>Фиксированная ширина Импорт</STRONG>, <STRONG>Экспорт запятые</STRONG>, <STRONG>Экспортировать Фиксированная ширина</STRONG>или <STRONG>Экспорт слияний Word для Windows</STRONG> передачи типы поддерживаются в проекте Microsoft Access (файлы с расширением ADP).</P>


<p></p></td>
</tr>
<tr class="even">
<td><p><strong>Имя спецификации</strong></p></td>
<td><p>Спецификация имя для набора параметров, который определяет, как импортировать или связанный текстовый файл. Фиксированная ширина текстового файла необходимо указать аргумент или использовать файл schema.ini на должно храниться в той же папке, что импортированные или связанный текстовый файл.</p>
<p>Чтобы создать спецификацию для Импорт или связывание текстового файла:</p>
<ol>
<li><p>В диалоговом окне <strong>Внешние данные</strong> введите путь к файлу исходного текста в поле <strong>имя файла</strong> .</p></li>
<li><p>Щелкните параметр, который требуется использовать для хранения данных (импорт, добавляемых или связь) и нажмите <strong>кнопку ОК</strong>.</p></li>
<li><p>В диалоговом окне <strong>Импорт текста</strong> нажмите кнопку <strong>Дополнительно</strong>.</p></li>
<li><p>Укажите параметры для данной спецификации, а затем нажмите кнопку <strong>Сохранить как</strong>.</p></li>
<li><p>Введите имя, которое необходимо использовать для спецификации, а затем нажмите <strong>кнопку ОК</strong>.</p></li>
<li><p>Для управления существующей спецификации, щелкнув <strong>спецификации</strong> в поле Спецификация.</p></li>
<li><p>Нажмите <strong>кнопку ОК</strong> , чтобы закрыть диалоговое окно Спецификация.</p></li>
</ol>
<p></p>
<p>Затем можно ввести имя спецификации в этот аргумент каждый раз, когда требуется для импорта или экспорта одного типа текстовый файл. Импорт, экспорт или ссылку на файлы с разделителями без ввода имени спецификация для этого аргумента. В этом случае Access использует параметры по умолчанию в диалоговом окне мастера. Access использует стандартный формат для файлов данных для слияния почты, поэтому нет необходимости введите имя спецификации для этого аргумента при экспорте этих типов файлов. Спецификации импорта/экспорта можно использовать с HTML-файлы, но только часть из спецификации, которая применяется такое спецификация для форматирования типа данных.</p></td>
</tr>
<tr class="odd">
<td><p><strong>Имя таблицы</strong></p></td>
<td><p>Имя таблицы Access для импорта данных текст для экспорта данных текста или связывание данных текст для. Можно также ввести имя запроса доступа, который требуется экспортировать данные из. Обязательный аргумент. Если нажать кнопку <strong>Импорта пробел</strong>, <strong>Фиксированная ширина импорта</strong>или <strong>Импорта HTML</strong> в поле <strong>Тип переноса</strong> , Access добавляет текстовые данные в этой таблице, если таблица уже существует. В противном случае Access создает новую таблицу, текстовые данные. Инструкции SQL нельзя использовать для указания данных для экспорта при использовании <strong>ImportExportText</strong> действие. Вместо использования инструкции SQL, необходимо сначала создать запрос и затем указать имя запроса в качестве аргумента <strong>Имя таблицы</strong> .</p></td>
</tr>
<tr class="even">
<td><p><strong>Имя файла</strong></p></td>
<td><p>Имя текстового файла для импорта, экспорта или ссылка на. Включите полный путь. Обязательный аргумент. Создается новый текстовый файл при экспорте данных из Access. Если имя файла — имя существующего текстового файла, Access заменяет существующий текстовый файл. Если вы хотите импортировать или связать определенной таблицы или списка в файле HTML, можно использовать аргумент <strong>Имя таблицы HTML</strong> .</p></td>
</tr>
<tr class="odd">
<td><p><strong>Содержит имена полей</strong></p></td>
<td><p>Указывает, содержит ли первая строка текстового файла имена полей. Если выбран вариант <strong>Да</strong>, Access использует имена в этой строке как имена полей таблицы Microsoft Access при Импорт или связывание данных текста. Если выбран вариант <strong>Нет</strong>, Access рассматривается как обычная строка данных первой строки. Значение по умолчанию: <strong>Нет</strong>. Access игнорирует этот аргумент для файлов данных для слияния почты Word для Windows, поскольку первая строка должна содержать имена полей. При экспортировании таблицы Access или выберите запроса, разделенный или фиксированная ширина текстового файла, Access вставляет имена полей таблицы или выберите запрос в первую строку текстового файла, если выбран <strong>Да</strong> для этого аргумента. Если которого выполняется импорт или связывание текстовый файл и выберите <strong>Да</strong> в этом поле, первая строка, содержащая имена полей необходимо использовать разделитель, задайте в спецификации импорта/экспорта для разделения имена полей. Экспорт в текстовый файл и нажмите кнопку <strong>Да</strong> для этого аргумента приложение Access вставляет имена полей в первую строку текстового файла с помощью этой разделителя.</p></td>
</tr>
<tr class="even">
<td><p><strong>Имя таблицы HTML</strong></p></td>
<td><p>Имя таблицы или списка в HTML-файл, который требуется импортировать или связать. Этот аргумент игнорируется, если аргумент <strong>Тип преобразования</strong> не задано значение импорта HTML или ссылку HTML. Если оставить это аргумент, первой таблицы или списка в HTML-файла импорта или связаны. Имя таблицы или списка в файле HTML, определяется текст, установленный с &lt;CAPTION&gt; тега, при наличии &lt;CAPTION&gt; тег. При наличии не &lt;CAPTION&gt; тег, имя определяется текст, установленный с &lt;заголовок&gt; тег. Если более одной таблицы или списка имеет то же имя, Access отличает их путем добавления номера в конце каждого имени; Например, Employees1 и «Сотрудники2».</p></td>
</tr>
<tr class="odd">
<td><p><strong>Кодировка страницы</strong></p></td>
<td><p>Имя набор символов, используемый в код страницы.</p></td>
</tr>
</tbody>
</table>


## <a name="remarks"></a>Примечания

Текстовые файлы можно экспортировать данные в запросах доступа. Microsoft Access экспортирует набор результатов запроса в виде таблицы.

Текстовые данные, добавляемые в существующую таблицу Access должны быть совместимы со структурой таблицы.

- Каждое поле в текст должен быть типу данных, то соответствующее поле в таблице.

- Поля должны быть в том же порядке (Если аргумент **Содержит имена полей** задано значение **Да**, в этом случае поле имена в тексте должны совпадать имена полей в таблице).

Это действие аналогично выберите **Текстовый файл** в **Импорт** или **Экспорт** группы на вкладке **Внешних данных** . Аргументы действие **ImportExportText** соответствуют параметрам в мастере работы с помощью команды **Текстовый файл** .

> [!TIP]
> Спецификации импорта/экспорта содержит сведения, необходимые Microsoft Access для импорта и экспорта текстового файла. Можно использовать сохраненные спецификаций на импорт, экспорт и связывание данных текст для следующего текстовые файлы. Например вы можете получить еженедельно объемы продаж в текстовый файл с большого компьютера. Можно создавать и сохранять спецификацию для этого типа данных и затем использовать спецификации при каждом добавлении эти данные в базу данных Access.

> [!NOTE]
> Запроса и фильтрации связанный текстовый файл запроса или фильтра с учетом регистра.

Чтобы выполнить действие **ImportExportText** в Visual Basic для приложений (VBA) модуль, используйте метод **ПреобразоватьТекст** **объекта** .

