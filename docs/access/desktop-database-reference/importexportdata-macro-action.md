---
title: ImportExportData Macro Action
TOCTitle: ImportExportData Macro Action
ms:assetid: 2cbde873-8a3d-b15c-4aab-405cddf44cea
ms:mtpsurl: https://msdn.microsoft.com/library/Ff192107(v=office.15)
ms:contentKeyID: 48543961
ms.date: 09/18/2015
mtps_version: v=office.15
f1_keywords:
- vbaac10.chm51789
f1_categories:
- Office.Version=v15
ms.openlocfilehash: d61d298da0e3b44895c8fce3ee1adc3b55d9a491
ms.sourcegitcommit: 19aca09c5812cfb98b68b5d4604dcaa814479df7
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 10/09/2018
ms.locfileid: "25482788"
---
# <a name="importexportdata-macro-action"></a>ImportExportData Macro Action

**Применимо к**: Access 2013 | Office 2013

Действие **ImportExportData** можно использовать для импорта или экспорта данных между текущей базы данных Access (MDB- или .accdb) или проект Microsoft Access (файлы с расширением ADP) и другой базы данных. Для баз данных Microsoft Access можно связать таблицы в текущей базе данных из другой базы данных. Связанные таблицы имеют доступ к данным таблицы оставив всю таблицу в другой базе данных.

> [!NOTE]
> 
		Эта действие не разрешено, если база данных не является доверенной. Дополнительные сведения о включении макросов см. по ссылкам в разделе See Also этой статьи.


## <a name="settings"></a>Параметры

Действие **ImportExportData** состоит из следующих аргументов.

<table>
<colgroup>
<col style="width: 50%" />
<col style="width: 50%" />
</colgroup>
<thead>
<tr class="header">
<th><p>Аргумент макрокоманды</p></th>
<th><p>Описание</p></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><p><strong>Тип передачи</strong></p></td>
<td><p>Тип переноса, который требуется сделать. Выберите <strong>Импорт</strong>, <strong>Экспорт</strong>или <strong>ссылку</strong> в поле <strong>Тип преобразования</strong> в разделе <strong>Действие аргументы</strong> в области построения макросов. Значение по умолчанию — <strong>Импорт</strong>.</p>

> [!NOTE]
> Тип передачи **связи** не поддерживается для проектов Access (файлы с расширением ADP).


<p></p></td>
</tr>
<tr class="even">
<td><p><strong>Тип базы данных</strong></p></td>
<td><p>Тип базы данных для импорта, экспорта или ссылка на. В поле <strong>Тип базы данных</strong> можно выбрать <strong>Microsoft Access</strong> или одного из других типов базы данных. Значение по умолчанию — <strong>Microsoft Access</strong>.</p></td>
</tr>
<tr class="odd">
<td><p><strong>Имя базы данных</strong></p></td>
<td><p>Имя базы данных для импорта, экспорта или ссылка на. Включите полный путь. Обязательный аргумент. Для типов баз данных, использующих отдельные файлы для каждой таблицы, таких как FoxPro, Paradox или dBASE введите в каталог, содержащий файл. Введите имя файла в аргументе <strong>источника</strong> (для импорта или связывания) или аргумент <strong>назначения</strong> (для экспорта). Для баз данных ODBC введите полный строка подключения Open Database Connectivity (ODBC).</p>
<p>Чтобы просмотреть пример строку подключения, ссылка на доступа к внешней таблицы:</p>
<ol>
<li><p>В диалоговом окне <strong>Внешние данные</strong> введите путь к исходной базы данных в поле <strong>имя файла</strong> .</p></li>
<li><p>Щелкните <strong>ссылку на источник данных путем создания связанной таблицы</strong>и нажмите <strong>кнопку ОК</strong>.</p></li>
<li><p>Выберите таблицу в диалоговом окне <strong>Связь с таблицами</strong> и нажмите <strong>кнопку ОК</strong>.</p></li>
</ol>
<p>Откройте недавно связанной таблицы в режиме конструктора и просмотра свойств таблицы, нажав кнопку <strong>Свойств</strong> на вкладке " <strong>Конструктор</strong> " в разделе <strong>средства</strong>. Текст в значение свойства <strong>Description</strong> — строка подключения для этой таблицы.</p>
<p>Дополнительные сведения о строках подключения ODBC можно файл справки или другая документация по драйвера ODBC этого типа базы данных ODBC.</p></td>
</tr>
<tr class="even">
<td><p><strong>Тип объекта</strong></p></td>
<td><p>Тип объекта для импорта или экспорта. Если выбран параметр <strong>Microsoft Access</strong> для аргумента <strong>Тип базы данных</strong> , можно выбрать <strong>таблицы</strong>, <strong>запроса</strong>, <strong>формы</strong>, <strong>отчета</strong>, <strong>макрос</strong>, <strong>модуль</strong>, <strong>Страницы доступа к данным</strong>, <strong>Представление</strong>, <strong> Схема</strong>, <strong>Хранимую процедуру</strong>или <strong>функцию</strong> в поле <strong>Тип объекта</strong> . Значение по умолчанию — <strong>в таблице</strong>. Если выбрать любой другой тип базы данных или выберите <strong>ссылку</strong> в поле <strong>Тип преобразования</strong> , этот аргумент игнорируется. При экспорте запроса к базе данных Microsoft Access, выберите <strong>Таблица</strong> в этот аргумент для экспорта набора результатов запроса и выберите <strong>запрос</strong> для экспорта самого запроса. При экспорте запроса в другой тип базы данных, этот аргумент игнорируется и экспортируется в наборе результатов запроса.</p></td>
</tr>
<tr class="odd">
<td><p><strong>Source</strong></p></td>
<td><p>Имя таблицы, запроса или объект для доступа к, который необходимо импортировать, экспортировать или связать. Для некоторых типов баз данных, таких как FoxPro, Paradox или dBASE это имя файла. Включите расширение имени файла (например, .dbf) в поле имя файла. Обязательный аргумент.</p></td>
</tr>
<tr class="even">
<td><p><strong>Назначения</strong></p></td>
<td><p>Имя импортированного, экспортированного или связанной таблицы, выборку или объекта Microsoft Access в целевой базы данных. Для некоторых типов баз данных, таких как FoxPro, Paradox или dBASE это имя файла. Включите расширение имени файла (например, .dbf) в поле имя файла. Обязательный аргумент. При выборе <strong>импорта</strong> в аргумент <strong>Тип преобразования</strong> и <strong>таблицы</strong> в аргументе <strong>Тип объекта</strong> доступа создается новая таблица с данными в импортированной таблице. При импорте таблицы или другого объекта, доступа к добавляет номер к имени, если конфликтует с существующим именем. Например при импорте сотрудников и сотрудникам уже существует, доступа к переименовывает импортируемой таблицы или Сотрудники1. При экспорте в базе данных Microsoft Access или другая база данных Access автоматически заменяет существующую таблицу или другой объект с тем же именем.</p></td>
</tr>
<tr class="odd">
<td><p><strong>Только структура</strong></p></td>
<td><p>Указывает, следует ли для импорта или экспорта только структуры таблицы базы данных без каких-либо его данных. Выберите <strong>Да</strong> или <strong>Нет</strong>. Значение по умолчанию: <strong>Нет</strong>.</p></td>
</tr>
</tbody>
</table>


## <a name="remarks"></a>Замечания

Можно импортировать и экспортировать таблицы между доступа и другие типы баз данных. Также можно экспортировать запросах доступа к другим типам баз данных. Microsoft Access экспортирует набор результатов запроса в виде таблицы. Можно импортировать и экспортировать все объекты базы данных Access, если обе базы данных, базы данных Access.

Если Импорт таблицы из другой базы данных Access (MDB- или .accdb), который является связанной таблицы в этой базе данных будет по-прежнему связан после его импорта. То есть, что импортируется связь, не всю таблицу.

Если базы данных, доступ к которым осуществляется требуется пароль, диалоговое окно отображается, когда запустить макрос. В этом диалоговом окне введите пароль.

Действие **ImportExportData** аналогична команды на вкладке " **Внешние данные** " в разделе **импорта** или **экспорта**. Эти команды можно использовать для выбора источника данных, например базы данных Access или другой тип базы данных, электронную таблицу или текстовый файл. Если выбрать базу данных, один или несколько диалоговых окон отображаются выберите тип объекта для импорта или экспорта (для баз данных Access), имя объекта и другие варианты, в зависимости от базы данных при импорте из или экспорте или создание ссылок на. Аргументы для действия **ImportExportData** соответствуют параметрам этих диалоговых окон.

Если вы хотите предоставить данные индекса для связанного dBASE таблицы, сначала связи с таблицей:

1.  Щелкните **файл dBASE**.

2.  В диалоговом окне **Внешние данные** введите путь к файлу dBASE в поле **имя файла** .

3.  Щелкните **ссылку на источник данных путем создания связанной таблицы**, а затем нажмите **кнопку ОК**.

4.  Укажите индексы в диалоговых окнах для этой команды. Для сохранения индекса данных в специальных INF-файл, находящийся в папке Microsoft Office.

5.  Вы можете удалить ссылки на связанные таблицы.

Следующий раз использовать действие **ImportExportData** для связи между этой таблицей dBASE используются сведения о индекса, заданного.

> [!NOTE]
> Если запроса или фильтра в связанной таблице запроса или фильтра с учетом регистра.

Чтобы выполнить действие **ImportExportData** в Visual Basic для приложений (VBA) модуль, используйте метод **ПреобразоватьБазуДанных** **объекта** .

