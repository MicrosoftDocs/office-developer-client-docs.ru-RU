---
title: Макрокоманда ImportExportData
TOCTitle: ImportExportData macro action
ms:assetid: 2cbde873-8a3d-b15c-4aab-405cddf44cea
ms:mtpsurl: https://msdn.microsoft.com/library/Ff192107(v=office.15)
ms:contentKeyID: 48543961
ms.date: 09/18/2015
mtps_version: v=office.15
f1_keywords:
- vbaac10.chm51789
f1_categories:
- Office.Version=v15
localization_priority: Normal
ms.openlocfilehash: e0cdf85461276d26005bc3066a387031a1086691
ms.sourcegitcommit: 8fe462c32b91c87911942c188f3445e85a54137c
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/23/2019
ms.locfileid: "32291889"
---
# <a name="importexportdata-macro-action"></a>Макрокоманда ImportExportData

**Область применения**: Access 2013, Office 2013

Действие **ImportExportData** можно использовать для импорта или экспорта данных между текущей базой данных Access (.mdb или .accdb) или проектом Access (.adp) и другой базой данных. Для баз данных Microsoft Access можно также связать таблицу с текущей базой данных Access из другой базы данных. Связь таблицы дает возможность получать доступ к данным в ней, хотя сама таблица остается в другой базе данных.

> [!NOTE]
> Эта макрокоманда доступна только для доверенных баз данных. 

## <a name="settings"></a>Параметры

Действие **ImportExportData** имеет следующие аргументы.

<table>
<colgroup>
<col style="width: 50%" />
<col style="width: 50%" />
</colgroup>
<thead>
<tr class="header">
<th><p>Аргумент макрокоманды</p></th>
<th><p>Описание</p></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><p><strong>Тип преобразования</strong></p></td>
<td><p>Нужный тип преобразования. В поле <strong>Тип преобразования</strong> в разделе <strong>Аргументы макрокоманды</strong> можно выбрать значение <strong>Импорт</strong>, <strong>Экспорт</strong> или <strong>Связь</strong>. Значение по умолчанию: <strong>Импорт</strong>.</p><p><strong>ПРИМЕЧАНИЕ.</strong> Тип преобразования <strong>Связь</strong> не поддерживается для проектов Access (ADP).</p></td>
</tr>
<tr class="even">
<td><p><strong>Тип базы данных</strong></p></td>
<td><p>Тип базы данных для импорта, экспорта или ссылки. Вы можете выбрать <strong>Microsoft Access</strong> или один из нескольких других типов баз данных в поле Тип <strong>базы данных.</strong> По умолчанию это <strong>Microsoft Access</strong>.</p></td>
</tr>
<tr class="odd">
<td><p><strong>Имя базы данных</strong></p></td>
<td><p>Имя базы данных для импорта, экспорта или ссылки. Следует указывать полный путь. Обязательный аргумент. Для типов баз данных, в которых для каждой таблицы используются отдельные файлы, такие как FoxPro, Paradox и dBASE, введите каталог, содержащий файл. Введите имя файла в <strong>аргументе Source</strong> (для импорта или ссылки) или <strong>аргументе Destination</strong> (для экспорта). Для баз данных ODBC введите полную строку подключения к открытой базе данных (ODBC).</p>
<p>Чтобы увидеть пример строки подключения, увязыв внешнюю таблицу с доступом:</p>
<ol>
<li><p>В <strong>диалоговом окне Get External Data</strong> введите путь базы данных источника в поле Имя <strong>файла.</strong></p></li>
<li><p>Щелкните <strong>Ссылку на источник данных, создав связанную таблицу,</strong>и нажмите <strong>кнопку ОК</strong>.</p></li>
<li><p>Выберите таблицу в <strong>диалоговом</strong> окне Таблицы ссылок и нажмите <strong>кнопку ОК</strong>.</p></li>
</ol>
<p>Откройте недавно связанную таблицу в представлении Design <strong></strong> и откройте свойства таблицы, щелкнув лист свойства на вкладке <strong>Design</strong> в <strong>статье Tools</strong>. Текст в <strong>параметре Свойства Description</strong> — это строка подключения для этой таблицы.</p>
<p>Дополнительные сведения о строках подключения ODBC см. в файле справки или другой документации для драйвера ODBC этого типа базы данных ODBC.</p></td>
</tr>
<tr class="even">
<td><p><strong>Object Type</strong></p></td>
<td><p>Тип объекта для импорта или экспорта. Если вы выбираете Microsoft <strong>Access</strong> для аргумента <strong>типа</strong> базы <strong></strong>данных, <strong></strong>вы можете выбрать таблицу, <strong></strong> <strong>запрос,</strong> <strong>форму,</strong>отчет, макрос, модуль, страницу доступа к данным, представление <strong>сервера,</strong>схему, сохраненную процедуру или функцию в поле <strong>Тип</strong> объекта. <strong></strong> <strong></strong> <strong></strong> <strong></strong> <strong></strong> Значение по умолчанию: <strong>Table</strong>. Если вы выбираете какой-либо другой тип базы данных или выберите <strong>Link</strong> в поле <strong>Типа</strong> передачи, этот аргумент игнорируется. Если вы экспортируете выбранный запрос в <strong></strong> базу данных Access, выберите таблицу в этом аргументе для экспорта набора результатов запроса и выберите <strong>Запрос</strong> для экспорта самого запроса. Если вы экспортируете выбранный запрос в другой тип базы данных, этот аргумент игнорируется, а набор результатов запроса экспортируется.</p></td>
</tr>
<tr class="odd">
<td><p><strong>Source</strong></p></td>
<td><p>Имя таблицы, выбор запроса или объекта Access, которые необходимо импортировать, экспортировать или ссылок. Для некоторых типов баз данных, таких как FoxPro, Paradox или dBASE, это имя файла. Включай расширение имени файла (например, dbf) в имя файла. Это обязательный аргумент.</p></td>
</tr>
<tr class="even">
<td><p><strong>Destination</strong></p></td>
<td><p>Имя импортируемой, экспортируемой или связанной таблицы, выберите запрос или объект Access в базе данных назначения. Для некоторых типов баз данных, таких как FoxPro, Paradox или dBASE, это имя файла. Включай расширение имени файла (например, dbf) в имя файла. Это обязательный аргумент. Если выбрать <strong>импорт в</strong> <strong>аргументе Типа</strong> передачи и таблице в аргументе <strong>Тип</strong> объекта, access создает новую таблицу, содержащую данные в импортируемой таблице. <strong></strong> Если вы импортируете таблицу или другой объект, Access добавляет номер в имя, если оно противоречит существующему имени. Например, если вы импортируете сотрудников и сотрудников уже существует, Access переименовает импортную таблицу или другой объект Employees1. Если вы экспортируете в базу данных Access или другую базу данных, Access автоматически заменяет любую существующую таблицу или другой объект с таким же именем.</p></td>
</tr>
<tr class="odd">
<td><p><strong>Только структура</strong></p></td>
<td><p>Указывает, импортировать или экспортировать только структуру таблицы баз данных без каких-либо данных. Выберите <strong>Да</strong> или <strong>Нет</strong>. По умолчанию используется значение <strong>Нет</strong>.</p></td>
</tr>
</tbody>
</table>


## <a name="remarks"></a>Примечания

Можно импортировать и экспортировать таблицы между Access и базами данных другого типа. Вы также можете экспортировать запросы на выборку Access к другим типам баз данных. Access экспортирует результирующий набор запроса в виде таблицы. Если обе базы данных являются базами данных Access, можно импортировать и экспортировать любые объекты базы данных Access.

Если вы импортируете таблицу из другой базы данных access (.mdb или .accdb), которая является связанной таблицей в этой базе данных, она по-прежнему будет связана после импорта. То есть импортируется связь, а не сама таблица.

Если для базы данных, к которой вы имеете доступ, требуется пароль, при запуске макроса появится диалоговое окно. Введите пароль в этом диалоговом окне.

Действие **ImportExportData** аналогично командам на вкладке **Внешние** данные в **статье Импорт** или **Экспорт.** Эти команды можно использовать для выбора источника данных, например базы данных Access или другого типа базы данных, электронной таблицы или текстового файла. При выборе базы данных появится один или несколько диалогов, в которых будет выбран тип объекта для импорта или экспорта (для баз данных доступа), имя объекта и другие параметры, в зависимости от базы данных, которую вы импортируете или экспортируете или ссылаете. Аргументы для действия **ImportExportData** отражают параметры в этих диалоговом окнах.

Если вы хотите предоставить сведения об индексе для связанной таблицы dBASE, сначала соединяем таблицу:

1.  Щелкните **dBASE File**.

2.  В **диалоговом окне Get External Data** введите путь для файла dBASE в поле Имя **файла.**

3.  Щелкните **Ссылку на источник данных, создав связанную таблицу,** а затем нажмите **кнопку ОК**.

4.  Укажите индексы в диалоговом окле для этой команды. Доступ сохраняет сведения об индексе в специальном файле (.inf), расположенном в Microsoft Office папке.

5.  Затем можно удалить ссылку на связанную таблицу.

При следующем использовании действия **ImportExportData** для привязки этой таблицы dBASE Access использует указанные данные индекса.

> [!NOTE]
> Если вы запрашиваете или фильтруете связанную таблицу, запрос или фильтр чувствительны к делу.

Чтобы выполнить действие **ImportExportData** в модуле Visual Basic для приложений (VBA), используйте метод **TransferDatabase** объекта **DoCmd.**

