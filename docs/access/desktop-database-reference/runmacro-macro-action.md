---
title: Макрокоманда RunMacro
TOCTitle: RunMacro macro action
ms:assetid: 25966f20-8160-0821-b88a-ed08b7786fdc
ms:mtpsurl: https://msdn.microsoft.com/library/Ff191868(v=office.15)
ms:contentKeyID: 48543787
ms.date: 09/18/2015
mtps_version: v=office.15
f1_keywords:
- vbaac10.chm43195
f1_categories:
- Office.Version=v15
localization_priority: Normal
ms.openlocfilehash: d9e86fb7d60af94d6ecde71b2a857a3cc5b9bcb8
ms.sourcegitcommit: d6695c94415fa47952ee7961a69660abc0904434
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 01/17/2019
ms.locfileid: "28712339"
---
# <a name="runmacro-macro-action"></a>Макрокоманда RunMacro

**Применимо к**: Access 2013, Office 2013

**ЗапускМакроса** можно использовать для запуска макроса. Макрос может входить в группу макросов.

Это действие можно использовать:

- Чтобы запустить макрос из другого макроса.

- Чтобы запустить макрос при выполнении определенного условия.

- Связывание макроса на настраиваемом меню команду.

## <a name="setting"></a>Setting

**ЗапускМакроса** имеет следующие аргументы.

<table>
<colgroup>
<col style="width: 50%" />
<col style="width: 50%" />
</colgroup>
<thead>
<tr class="header">
<th><p>Аргумент макрокоманды</p></th>
<th><p>Описание</p></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><p><strong>Имя макроса</strong></p></td>
<td><p>Имя макроса для запуска. В поле <strong>Имя макроса</strong> в разделе <strong>Действие аргументы</strong> в области построения макросов показывает все макросы (и группы макросов) в текущей базе данных. Если макрос находится в группе макросов, оно указано в разделе имя группы в списке как <em>ИмяГруппыМакросов</em>. <em>имяМакроса</em>. Обязательный аргумент. Если макрос, содержащий <strong>ЗапускМакроса в базе данных библиотеки</strong> , Microsoft Access выполняет поиск макроса с этим именем в базе данных библиотеки и не проводит поиск в текущей базе данных.</p></td>
</tr>
<tr class="even">
<td><p><strong>Число повторов</strong></p></td>
<td><p>Максимальное количество попыток запуска макроса. Если оставить этот аргумент пустым (и <strong>Повторите выражения</strong> -аргумента также не задан), макрос выполняется один раз.</p></td>
</tr>
<tr class="odd">
<td><p><strong>Повторите выражение</strong></p></td>
<td><p>Выражение, которое оценивается как <strong>значение True</strong> (– 1) или <strong>False</strong> (0). Выполнение макроса останавливается, если выражение имеет значение <strong>False</strong>. Выражение каждый раз при выполнении макроса.</p></td>
</tr>
</tbody>
</table>

## <a name="remarks"></a>Замечания

Если ввести имя группы макросов для аргумента **Имя макроса** доступа запуска первого макроса в группе макросов.

Это действие аналогично выберите **Выполнить макрос** на вкладке **Инструменты для баз данных** , выберите макрос и нажмите кнопку **ОК**. Тем не менее эта команда выполняется макрос только один раз, тогда как макрос можно запустить **ЗапускМакроса** столько раз.

> [!TIP]
> Чтобы определить, сколько раз запускает макрос можно использовать **Число повторов** и **повторите выражение** аргументы:
> - Если оба аргумента оставлено пустым, макрос выполняется один раз.
> - Если ввести номер **Число повторов** , но оставить пустым **Повторите выражение** , макрос выполняется указанное число раз.
> - Если оставить пустым **Число повторов** , но введите выражение для **Повторите выражение**, макрос выполняется только после вычисление выражения дает значение **False**.
> - При вводе значения обоих аргументов, запускается макрос количество времени, указанного в **Число повторов** или пока **Условие повтора** вычисляющий значение **False**, что происходит первым.

Если макрос, содержащий **ЗапускМакроса** достигает **ЗапускМакроса** , Access запускает макрос называемое. По завершении вызванного макроса доступа возвращает исходное макрос и выполняется следующее действие.

> [!NOTE]
> - Можно вызвать макроса в той же группе макросов или в другой группы макросов.
> - Можно вкладывать макросов. То есть можно запустить макрос A, которая, в свою очередь вызывает макрос B и т. д. По завершении вызванного макроса в каждом случае доступа возвращает макрос, который вызвал ее и выполняется следующее действие в этот макрос.

Чтобы запустить **ЗапускМакроса** в Visual Basic для приложений (VBA) модуль, используйте метод **RunMacro** **объекта** .

