---
title: Интерфейс Application (OneNote)
manager: soliver
ms.date: 03/09/2015
ms.audience: Developer
ms.topic: overview
localization_priority: Normal
ms.assetid: 87926f7d-e1dc-41d5-8805-6ba91fc7b154
description: 'Интерфейс приложения включает в себя методы Получение справки, редактирования и обновления данных OneNote и контента. Методы, в четыре общие категории:'
ms.openlocfilehash: 25bb1aa570f6c36aa04140d9256d277bee65152b
ms.sourcegitcommit: 9d60cd82b5413446e5bc8ace2cd689f683fb41a7
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 06/11/2018
ms.locfileid: "19807655"
---
# <a name="application-interface-onenote"></a>Интерфейс Application (OneNote)

Интерфейс **приложения** включает в себя методы Получение справки, редактирования и обновления данных OneNote и контента. Методы, в четыре общие категории: 
  
- **Структура записная книжка** &ndash; Методы для работы со структурой записной книжки, в том числе для обнаружения, открытие, изменение, закрытие и удаление записные книжки, группы разделов и разделы. 
    
- **Контент страницы** &ndash; Методы для работы со страницы и страницы содержимого, в том числе для обнаружения, изменение, сохранение и удаление содержимого страницы. Контент страницы включает в себя двоичные объекты, такие как рукописного ввода и изображения, а текстовые объекты, такие как файлов в режиме структуры. 
    
- **Навигация** &ndash; Методы для поиска, создание ссылок на и переход к страницам и объекты. 
    
- **Функциональные** &ndash; Все другие методы, которые выполнения определенных действий или задать параметры в OneNote. 
    
Кроме того интерфейс **приложения** включает в себя ряд *свойств* и *событий* . 
  
## <a name="notebook-structure-methods"></a>Методы структур записная книжка
<a name="ON14DevRef_Application_NotebookStructure"> </a>

Методы, описанные в этом разделе позволяют обнаружения, открытие, изменение, закройте и удаление записных книжек OneNote, группы разделов и разделы.
  
### <a name="gethierarchy-method"></a>Метод GetHierarchy

|||
|:-----|:-----|
|**Описание** <br/> |Получает записной книжки узел структуру иерархии, начиная с узла указан (всех записных книжек или одной записной книжки, раздел группы или раздел) и расширение вниз на всех потомков указан на уровне.  <br/> |
|**Синтаксис** <br/> | `HRESULT GetHierarchy(`<br/>`[in]BSTR bstrStartNodeID,`<br/>`[in]HierarchyScope hsScope,`<br/>`[out]BSTR * pbstrHierarchyXmlOut,`<br/>`[in,defaultvalue(xs2013)]XMLSchema xsSchema);` <br/> |
|**Параметры** <br/> | _bstrStartNodeID_ &ndash; Узел (записной книжки, группы или раздела) которого вы хотите потомков. Если передать пустую строку ("»), метод возвращает все узлы ниже корневого узла (то есть, все записные книжки, группы разделов и разделы). При указании записной книжки, группе или узел раздела, метод возвращает только потомки этого узла.  <br/><br/>_hsScope_ &ndash; Наименьший уровень дочернего узла требуется. Например при указании страниц, метод возвращает все узлы как можно ближе как уровне страницы. При указании разделов, метод возвращает только узлы раздел под записной книжки. Дополнительные сведения см в перечислении **HierarchyScope** в разделе [перечисления](enumerations-onenote-developer-reference.md#odc_HierarchyScope) .  <br/><br/>_pbstrHierarchyXmlOut_ &ndash; (Выходной параметр) A указатель на строку, в которой вы не нужно записывать выходные данные XML.  <br/><br/>_xsSchema_ &ndash; (Необязательно) версии OneNote XML-схему, тип **XMLSchema**, нужно быть выходные данные. Можно указать, следует ли XML-схему версии 2013, 2010, 2007, или текущей версии.  <br/><br/>**Примечание**: рекомендуется использовать указание версии OneNote (например, **xs2013**), а не с помощью **xsCurrent** или оставить пустым, так как это позволит надстройки для работы с будущими версиями OneNote.           |
   
Метод GetHierarchy возвращает строку в формате OneNote 2013 XML по умолчанию, либо можно задать основной версии схемы XML с помощью параметра необязательно _xsSchema_ . 
  
В зависимости от параметров, передаваемого метод **GetHierarchy** может возвращать списки (например, всех записных книжек, все разделы в всех записных книжек, все страницы в данном разделе или всех страниц в пределах указанной записной книжки). Для каждого узла XML строки, возвращаемой предоставляет свойства (например, заголовок раздела или страницы, идентификатор и времени последнего изменения). 
  
Не все сочетания начальный узел и области являются допустимыми. Например при указании раздел запуска узел и область записной книжки, **GetHierarchy** возвращает значение null, так как записной книжке в иерархии узлов выше раздел. 
  
В следующем примере C# показано, как использовать метод **GetHierarchy** для получения всей иерархии OneNote, включая все записные книжки, до уровня страницы. Строка выходные данные копируются в буфер обмена, откуда можно вставить строку в текстовый редактор для ознакомления. 
  
```cs
static void GetEntireHierarchy()
    {
        String strXML;
        OneNote.Application onApplication = new OneNote.Application();
        onApplication.GetHierarchy(null, 
            OneNote.HierarchyScope.hsPages, out strXML);
        Clipboard.SetText(strXML);
        MessageBox.Show("The XML has been copied to the clipboard");
    }

```

### <a name="updatehierarchy-method"></a>Метод UpdateHierarchy

|||
|:-----|:-----|
|**Описание**|Изменяет или обновляет иерархии записных книжек. Например можно добавить разделы и группы разделов в записной книжке Добавление новой записной книжки, Перемещение разделов в записной книжке, измените имя раздела, добавьте в раздел страницы или изменить порядок страниц в разделах.|
|**Синтаксис**| `HRESULT UpdateHierarchy(`<br/>`[in]BSTR bstrChangesXmlIn,`<br/>`[in,defaultvalue(xsCurrent)] XMLSchema xsSchema);`|
|**Параметры**| _bstrChangesXmlIn_ &ndash; Строка, содержащая код OneNote XML, указывающее, чтобы сделать изменения иерархии. Например если вы хотите добавить новый раздел, можно добавить элемент **раздела** в XML-строку, чтобы указать, где будет новый раздел для добавления. Кроме того Если вы хотите изменить имя существующего раздела, можно прежнего идентификатора раздела и измените его атрибут **имени** в XML-код.<br/><br/>_xsSchema_ &ndash; The OneNote (необязательно) версия схемы строка _bstrChangesXmln_. Это необязательный значение используется для указания версии OneNote XML-схему, _bstrChangesXmlIn_ строки. Если это значение не указано, OneNote будет предполагать, что XML-код находится в версии схемы _xsCurrent_. <br/><br/>**Примечание**: рекомендуется использовать указание версии OneNote (например, **xs2013**), а не с помощью **xsCurrent** или оставить пустым, так как это позволит надстройки для работы с будущими версиями OneNote.           |
   
Если передать только частичный строку OneNote XML для параметра _bstrChangesXmlIn_ OneNote пытается определить необходимые изменения. Например если указан элемент **записной книжки** , который содержит только один раздел OneNote добавляет разделе после любые существующие разделы. Тем не менее если операция указываемые является неоднозначным, результат может быть трудно определить. Например если существующие записная книжка содержит четыре раздела и XML-строка, передаваемого включает записной книжки и только четвертый и первый раздел (в таком порядке), OneNote поместить второго и третьего разделов до четвертый раздел или после первого раздела . 
  
Метод **UpdateHierarchy** нельзя использовать для удаления части записной книжки. То есть передав XML-строка, которая включает в себя только часть из существующей иерархии не удаляет разделы, которые не включены в строке. Чтобы удалить частью иерархии, используйте метод **DeleteHierarchy** . 
  
Следующий код C# показано одним из способов использования метода **UpdateHierarchy** для изменения иерархии OneNote с помощью изменения имени существующий раздел. Считывает XML-код из примера файла с именем ChangeSectionName.xml в корне диска C, загружает в XML-документ и затем передает XML структуры документа в метод. 
  
```cs
static void UpdateExistingHierarchy()
    {
        OneNote.Application onApplication = new OneNote.Application();
        
        // Get the XML from the file.
        XmlTextReader reader = new XmlTextReader("C:\\ChangeSectionName.xml");
        reader.WhitespaceHandling = WhitespaceHandling.None;
        XmlDocument xmlDocIn = new XmlDocument();
        xmlDocIn.Load(reader);
        
        // Update the hierarchy.
        onApplication.UpdateHierarchy(xmlDocIn.OuterXml,
        OneNote.XMLSchema.xs2007);   
    }

```

Следующий XML-код приведен фрагмент файла ChangeSectionName.xml, который передает предыдущего кода C# метод. Когда XML-код передается методу **UpdateHierarchy** , он изменяется имя одного из разделов в существующую иерархию (, изменив значение атрибута **name** «Мои переименовать раздел»). Затем удаляет все разделы за исключением того, имя которого был изменен. Кроме того код удаляет ненужные атрибуты из целевого **раздел** элемента, включая атрибуты **lastModifiedTime**, **isCurrentlyViewed**и **цвет** , оставив только **имя**, **идентификатор**и ** путь** атрибуты без изменений. 
  
```XML
<?xml version="1.0" ?> 
    <one:Notebooks xmlns:one="http://schemas.microsoft.com/office/onenote/12/2004/onenote"> 
        <one:Notebook name="My Notebook" nickname="My Notebook" ID="{0B8E7305-AC2C-4BCB-8651-1CDA55AAE14C}{1}{B0}"> 
            <one:Section name="My Renamed Section" ID="{5F4E2908-44BA-4C02-91FE-49FC665E9A33}{1}{B0}" path="C:\My Section.one" /> 
        </one:Notebook> 
    </one:Notebooks>
```

С помощью кода, показано в примере для метода **GetHierarchy** , который изменяется, как показано ниже, для ограничения области к разделам был получен приведенный выше XML-код. 
  
```cs
static void GetAllSections()
    {
        String strXML;
        OneNote.Application onApplication = new OneNote.Application();
        onApplication.GetHierarchy(System.String.Empty, 
            OneNote.HierarchyScope.hsSections, out strXML);
        Clipboard.SetText(strXML.ToString());
        MessageBox.Show("The XML has been copied to the Clipboard");
    }

```

В следующем примере C# показано полное консольное приложение, которое выполняет поиск раздела с именем " `Sample_Section`«, запрос на ввод нового имени для того раздела, а затем использует метод **UpdateHierarchy** Чтобы изменить имя раздела имя, которое пользователь типизированный. Перед выполнением кода, измените " `Sample_Section`" к имени раздел, в котором существует в иерархии OneNote.
  
```cs
    static void Main(string[] args)
    {
        
        // OneNote 2013 Schema namespace.
        string strNamespace = "http://schemas.microsoft.com/office/onenote/2013/onenote";
        string outputXML;
        Application onApplication = new Application();
        onApplication.GetHierarchy(null, HierarchyScope.hsSections, out outputXML);
        // Load a new XmlDocument.
        XmlDocument xmlDoc = new XmlDocument();
        xmlDoc.LoadXml(outputXML);
        XmlNamespaceManager nsmgr = new XmlNamespaceManager(xmlDoc.NameTable);
            nsmgr.AddNamespace("one", strNamespace);
        // Search for the section named "Sample_Section".
        XmlNode xmlNode = xmlDoc.SelectSingleNode("//one:Section[@name='Sample_Section']", nsmgr);
        // Prompt for a new section title.
        System.Console.Write("Please enter a new title for the section: ");
        string input = System.Console.ReadLine();
        xmlNode.Attributes["name"].Value = input; 
        // Update the section with the new title.
        onApp.UpdateHierarchy(xmlNode.OuterXml);
        System.Console.Write("Done!\n");
    }

```

### <a name="openhierarchy-method"></a>Метод OpenHierarchy

|||
|:-----|:-----|
|**Описание** <br/> |Откроется раздел группы или раздела, который указан.  <br/> |
|**Синтаксис** <br/> | `HRESULT OpenHierarchy(`<br/>`[in]BSTR bstrPath,`<br/>`[in]BSTR bstrRelativeToObjectID,`<br/>`[out]BSTR * pbstrObjectID,`<br/>`[in,defaultvalue(cftNone)]CreateFileType cftIfNotExist);` <br/> |
|**Параметры** <br/> | _bstrPath_ &ndash; Пути, который необходимо открыть. Записной книжке или группы разделов в записной книжке _bstrPath_ может быть путь к папке или путь к файлу раздела .one. Если указать путь к файлу раздела .one необходимо включить расширение .one в строке путь к файлу.  <br/><br/>_bstrRelativeToObjectID_ &ndash; OneNote идентификатор родительского объекта, с которой будет выполняться новый объект, чтобы открыть (записная книжка или раздел группы). Если параметр _bstrPath_ абсолютный путь, вы можете передать пустую строку ("») для _bstrRelativeToObjectID_. Кроме того вы можете передать идентификатор объекта группы записная книжка или раздел, который должен содержать объект (раздел или группу разделов), который вы хотите создать, а затем укажите имя файла (например, section1.one) объекта, который вы хотите создать в группе родительский объект.  <br/><br/>_pbstrObjectID_ &ndash; (Выходной параметр), которое возвращает OneNote для записной книжки, группы или раздела, метод **OpenHierarchy** открывает идентификатор объекта. Этот параметр является указатель на строку, в которую необходимо метода для записи с идентификатором.  <br/><br/>_cftlfNotExist_ &ndash; Перечислимые значение из перечисления [CreateFileType](enumerations-onenote-developer-reference.md#odc_CreateFileType) (не обязательно). При передаче значения для _cftIfNotExist_метод **OpenHierarchy** создает группу разделов или раздел файла по указанному пути только в том случае, если файл не существует.  <br/> |
   
При указании группы раздела, который не является в open записной книжки, метод **OpenHierarchy** открывает группу разделов в записной книжке. При указании раздела, который не является в open записной книжки, метод **OpenHierarchy** открывает разделе в группе раздел последних открытых разделах. При указании группы раздела или раздела, который уже присутствует в open записной книжки, ничего не происходит, так как группа разделов или раздел уже открыт, а также. В любом случае **OpenHierarchy** возвращает идентификатор объекта для группы разделов, записной книжки или раздела, который требуется указать, которые можно использовать в других операций. 
  
Можно также использовать метод **OpenHierarchy** для создания новых раздела, вместо этого путем импорта XML. 
  
Приведенный ниже код показано, как использовать метод **OpenHierarchy** для откройте раздел собраний в записной книжке работы и получить идентификатор для раздела. Раздел еще не существует, OneNote создает в указанном вами месте. 
  
```cs
static void OpenSection()
    {
        String strID;
        OneNote.Application onApplication = new OneNote.Application();
        onApplication.OpenHierarchy("C:\\Documents and Settings\\user\\My Documents\\OneNote Notebooks\\Work\\Meetings.one", 
        System.String.Empty, out strID, OneNote.CreateFileType.cftSection);
    }

```

### <a name="deletehierarchy-method"></a>Метод DeleteHierarchy

|||
|:-----|:-----|
|**Описание** <br/> |Удаляет любой объект иерархии (группы раздела, раздел или страницы) в иерархии записной книжки OneNote.  <br/> |
|**Синтаксис** <br/> | `HRESULT DeleteHierarchy(`<br/>`[in]BSTR bstrObjectID,`<br/>`[in,defaultvalue(0)]DATE dateExpectedLastModified,`<br/>`[in,defaultvalue(false)]VARIANT_BOOL deletePermanently);` <br/> |
|**Параметры** <br/> | _bstrObjectID_ &ndash; OneNote идентификатор объекта, который требуется удалить. Объект может быть группы раздела, раздел или страницы.  <br/><br/>_dateExpectedLastModified_ &ndash; (Необязательно) Дата и время, которые вы думаете, объект, необходимо удалить Дата и время последнего изменения. Если передать ненулевое значение для этого параметра, OneNote продолжается с пакетом обновления, только в том случае, если значение, передаваемого соответствует фактическая дата и время последнего изменения объекта. Передача значения для этого параметра предотвращает случайной перезаписи изменения пользователей, с момента последнего изменения объекта.  <br/><br/>_deletePermanently_ &ndash; (Необязательно) **значение true,** Чтобы окончательно удалить содержимого; **значение false** для перемещения содержимого в OneNote корзины для соответствующего записной книжки (по умолчанию). Если записной книжки OneNote 2007 формат, не recycle bin, существует, поэтому содержимое удаляется без возможности восстановления.  <br/> |
   
### <a name="createnewpage-method"></a>Метод CreateNewPage

|||
|:-----|:-----|
|**Описание** <br/> |Добавляет новую страницу раздела, который указан. Новая страница добавляется в качестве последней странице раздела  <br/> |
|**Синтаксис** <br/> | `HRESULT CreateNewPage(`<br/>`[in]BSTR bstrSectionID,`<br/>`[out]BSTR * pbstrPageID);`<br/>`[in,defaultvalue(npsDefault)]NewPageStyle npsNewPageStyle);` <br/> |
|**Параметры** <br/> | _bstrSectionID_ &ndash; Строка, содержащая идентификатор раздела, в котором вы хотите создать новую страницу OneNote.  <br/><br/>_pbstrPageID_ &ndash; (Выходной параметр) A указатель на строку, в которую метод записывает идентификатор OneNote для только что созданный страницы.  <br/><br/>_npsNewPageStyle_ &ndash; Значение из перечисления **NewPageStyle** , который определяет стиль страницы, чтобы создать.  <br/> |
   
API-интерфейса OneNote включает метод **CreateNewPage** для удобства. Можно предоставить тем же больше возможностей расположение новой страницы в иерархии, путем вызова метода **UpdateHierarchy** управления. Метод **UpdateHierarchy** также позволяет создавать вложенные страницы в то же время, при создании новой страницы. 
  
### <a name="closenotebook-method"></a>Метод CloseNotebook

|||
|:-----|:-----|
|**Описание** <br/> |Закрывает указанного записной книжки.  <br/> |
|**Синтаксис** <br/> | `HRESULT CloseNotebook(`<br/>`[in]BSTR bstrNotebookID,`<br/>`[in,defaultvalue(false)]VARIANT_BOOL force);` <br/> |
|**Параметры** <br/> | _bstrNotebookID_ &ndash; OneNote идентификатор записной книжки, которую нужно закрыть.  <br/><br/>_Принудительное_ &ndash; (Необязательно) **значение true** для закрытия записной книжки, даже в том случае, если есть изменения в записной книжке, который не удается синхронизировать перед закрытием; в противном случае — **значение false** (значение по умолчанию).  <br/> |
   
Чтобы закрыть записной книжки, указываемые можно использовать метод **CloseNotebook** . При вызове этого метода OneNote синхронизирует любые автономные файлы с содержимым текущей записной книжки, при необходимости, после чего указанного записной книжки. После завершения выполнения метода, записная книжка больше не отображается в списке открытых записных книжек OneNote пользовательского интерфейса (UI). 
  
### <a name="gethierarchyparent-method"></a>Метод GetHierarchyParent

|||
|:-----|:-----|
|**Описание** <br/> |Получает идентификатор OneNote для родительский объект объекта OneNote.  <br/> |
|**Синтаксис** <br/> | `HRESULT GetHierarchyParent (`<br/>`[in]BSTR bstrObjectID,`<br/>`[out]BSTR * pbstrParentID);` <br/> |
|**Параметры** <br/> | _bstrObjectID_ &ndash; Строка, содержащая идентификатор OneNote в объект, который требуется найти родительский объект.  <br/><br/>_pbstrParentID_ &ndash; (Выходной параметр) A указатель на строку, в которую метод записывает OneNote идентификатор родительского объекта.  <br/> |
   
Если объект OneNote не родительский объект (например, когда требуется получить родительского записной книжки), — это исключение.
  
### <a name="getspeciallocation-method"></a>Метод метода GetSpecialLocation

|||
|:-----|:-----|
|**Описание** <br/> |Находит путь к расположению, где OneNote хранит некоторые специальные элементы, например резервных копий, раздела и записной книжки по умолчанию.  <br/> |
|**Синтаксис** <br/> | `HRESULT GetSpecialLocation(`<br/>`[in]SpecialLocation slToGet,`<br/>`[out]BSTR * pbstrSpecialLocationPath);` <br/> |
|**Параметры** <br/> | _slToGet_ &ndash; Одно из значений перечисления [SpecialLocation](enumerations-onenote-developer-reference.md#odc_SpecialLocation) , указывает расположение специальную папку для получения.  <br/><br/>_pbstrSpecialLocationPath_ &ndash; (Выходной параметр) A указатель на строку, в которую необходимо OneNote для записи путь специальную папку.  <br/> |
   
Этот метод можно использовать для определения местоположения на диске в папку раздела. — Это папка, в которой хранятся OneNote заметки, созданные при перетаскивании элемента в OneNote, а также примечания непосредственно из других приложений (например, при нажатии кнопки **Отправить в OneNote** в Microsoft Outlook или Microsoft Internet Explorer). 
  
## <a name="page-content-methods"></a>Методы содержимого страницы
<a name="ON14DevRef_Application_PageContent"> </a>

Методы, описанные в этом разделе позволяют для обнаружения, обновлять и удалять содержимое на страницы в записных книжек OneNote, а также для публикации содержимого OneNote.
  
### <a name="getpagecontent-method"></a>Метод GetPageContent

|||
|:-----|:-----|
|**Описание**|Получает все содержимое (в формате OneNote XML) заданной страницы.|
|**Синтаксис**| `HRESULT GetPageContent(`<br/>`[in]BSTR bstrPageID,`<br/>`[out]BSTR * pbstrPageXmlOut,`<br/>`[in,defaultvalue(piBasic)]PageInfo pageInfoToExport,`<br/>`[in,defaultvalue(xsCurrent)]XMLSchema xsSchema);`|
|**Параметры**| _bstrPageId_ &ndash; OneNote идентификатор страницы, содержимое которого требуется получить.<br/><br/>_pbstrPageXmlOut_ &ndash; (Выходной параметр) A указатель на строку, в которой вы не нужно записывать выходные данные XML.<br/><br/>_pageInfoToExport_ &ndash; (Необязательно) указывает, будет ли метод **GetPageContent** возвращает двоичные данные, встроенные в XML-код и кодировка base-64. Двоичные содержимое может включать, например, изображений и рукописного ввода данных. Параметр _pageInfoToExport_ Указывает необходимость пометки выделения в XML-код, возвращающий метода **GetPageContent** . Требуемое значение перечисления из перечисления [PageInfo](enumerations-onenote-developer-reference.md#odc_PageInfo) .<br/><br/>_xsSchema_ &ndash; (Необязательно) версии OneNote XML-схему, тип **XMLSchema**, нужно быть выходные данные. Можно указать, следует ли XML-схему версии 2013, 2010, 2007, или текущей версии. <br/><br/>**Примечание**: рекомендуется использовать указание версии OneNote (например, **xs2013**), а не с помощью **xsCurrent** или оставить пустым, так как это позволит надстройки для работы с будущими версиями OneNote.           |
   
По умолчанию Чтобы избежать лишних длина в XML-строку, возвращенную им, OneNote не включает двоичные данные в XML-код. По той же причине она не помечать текущее выделение с помощью выбора атрибутов. Двоичные объекты включают OneNote код в тегах. Чтобы получить двоичных объектов, вызовите метод **GetBinaryPageContent** и передайте ему идентификатор OneNote, вы получаете из метода **GetPageContent** . Метод **GetPageContent** используется в том случае, когда нет необходимости сразу же двоичных данных. 
  
### <a name="updatepagecontent-method"></a>Метод UpdatePageContent

|||
|:-----|:-----|
|**Описание**|Обновление или изменение содержимого на странице.|
|**Синтаксис**| `HRESULT UpdatePageContent(`<br/>`[in]BSTR bstrPageChangesXmlIn,`<br/>`[in,defaultvalue(0)]DATE dateExpectedLastModified,`<br/>`[in,defaultvalue(xsCurrent)]XMLSchema xsSchema,`<br/>`[in,defaultvalue(false)]VARIANT_BOOL force);`|
|**Параметры**| _bstrPageChangesXmlIn_ &ndash; Строка, содержащая код OneNote XML, которая включает в себя изменения, который требуется сделать на страницу.<br/><br/>_dateExpectedLastModified_ &ndash; (Необязательно) Дата и время, которые вы думаете, страницы, которую требуется обновить Дата и время последнего изменения. Если передать ненулевое значение для этого параметра, OneNote продолжается с пакетом обновления, только в том случае, если значение, передаваемого соответствует фактическая дата и время последнего изменения страницы. Передача значения для этого параметра предотвращает случайной перезаписи изменения пользователей, с момента последнего изменения страницы.<br/><br/>_xsSchema_ &ndash; (Необязательно) версии OneNote XML-схему, тип **XMLSchema**, нужно быть выходные данные. Можно указать, следует ли XML схемы версии 2013, 2010, 2007, или текущей версии. <br/><br/>**Примечание**: рекомендуется использовать указание версии OneNote (например, **xs2013**), а не с помощью **xsCurrent** или оставить пустым, так как это позволит надстройки для работы с будущими версиями OneNote.<br/><br/>_Принудительное_ (Необязательно) **значение true,** чтобы обновить контент страницы, даже при наличии неизвестные данные на странице в будущих версиях OneNote; в противном случае — **значение false** (значение по умолчанию). |
   
Этот метод можно использовать для изменения страницы разными способами. Например можно использовать метод **UpdatePageContent** Добавление структуры на страницу, изменять содержимое структуры, добавление изображений, добавлять рукописного ввода, Перемещение содержимого или изменение текста в файлов в режиме структуры. 
  
В качестве примера более конкретные может использование метода **GetPageContent** для экспорта существующей страницы, внести изменения в XML-коде для страницы и затем использовать метод **UpdatePageContent** попытку импорта всей страницы. Или можно использовать этот метод для добавления новых объектов страницы, такие как изображения, в нижней части существующей страницы. 
  
Только объекты, которые необходимо включить в XML-код, передайте методу **UpdatePageContent** являются объектов на уровне страницы (например, файлов в режиме структуры, изображений на странице или рукописного ввода на странице ""), которые были изменены. Этот метод не изменять или удалять объекты уровня страницы, которые не указан в параметре _bstrPageChangesXmlIn_ . Метод полностью заменяет объектов на уровне страницы, например файлов в режиме структуры, чьи идентификаторы совпадают с объектами, передаваемого. Следовательно необходимо полностью указать все объекты уровня страницы в коде, включая их существующего контента и изменения, которые необходимо внести в них. 
  
К примеру Если страница содержит структуру и фонового изображения страницы, можно замените структуры и оставьте без изменений, полностью указание структуры в XML-код, с помощью идентификатора существующей структуры и без учета изображения в коде изображение. Так как оптимизированная структуры, включаемых в коде полностью заменяет существующий структуры, необходимо включить все содержимое структуры.
  
Кроме того метод **UpdatePageContent** изменяет только свойства элемента, указанные в параметре _bstrPageChangesXmlIn_ . Например при указании некоторые, но не все свойства элемента **PageSettings** свойства, которые не указан не изменяются. 
  
Следующем примере показано, как использовать метод **UpdatePageContent** для изменения заголовка страницы и добавьте некоторые образец текста на страницу. Перед выполнением кода, замените допустимый код идентификатор страницы, показано в примере кода, чтобы код работает на вашем компьютере. Вы можете получить идентификатор страницы для страницы, с помощью метода **GetHierarchy** и анализ результатов. 
  
```cs
static void UpdatePageContent()
    {
        OneNote.Application onApplication = new OneNote.Application();
        String strImportXML;
        strImportXML = "<?xml version=\"1.0\"?>" +
            "<one:Page xmlns:one=\"http://schemas.microsoft.com/office/onenote/12/2004/onenote\" 
            ID=\"{3428B7BB-EF39-4B9C-A167-3FAE20630C37}{1}{B0}\">" +
            "    <one:PageSettings RTL=\"false\" color=\"automatic\">" +
            "        <one:PageSize>" +
            "            <one:Automatic/>" +
            "        </one:PageSize>" +
            "        <one:RuleLines visible=\"false\"/>" +
            "    </one:PageSettings>" +
            "    <one:Title style=\"font-family:Calibri;
                 font-size:17.0pt\" lang=\"en-US\">" +
            "        <one:OE alignment=\"left\">" +
            "            <one:T>" +
            "                <![CDATA[My Sample Page]]>" +
            "            </one:T>" +
            "        </one:OE>" +
            "    </one:Title>" +
            "    <one:Outline >" +
            "        <one:Position x=\"120\" y=\"160\"/>" +
            "        <one:Size width=\"120\" height=\"15\"/>" +
            "        <one:OEChildren>" +
            "            <one:OE alignment=\"left\">" +
            "                <one:T>" +
            "                    <![CDATA[Sample Text]]>" +
            "                </one:T>" +
            "            </one:OE>" +
            "        </one:OEChildren>" +
            "    </one:Outline>" +
            "</one:Page>";
        // Update the page content.
        onApplication.UpdatePageContent(strImportXML, System.DateTime.MinValue);
    }

```

### <a name="getbinarypagecontent-method"></a>Метод GetBinaryPageContent

|||
|:-----|:-----|
|**Описание** <br/> |Возвращает двоичных объектов, таких как рукописного ввода или изображений на страницу OneNote как строка в кодировке base 64.  <br/> |
|**Синтаксис** <br/> | `HRESULT GetBinaryPageContent(`<br/>`[in]BSTR bstrPageID,`<br/>`[in]BSTR bstrCallbackID,`<br/>`[out]BSTR * pbstrBinaryObjectB64Out);` <br/> |
|**Параметры** <br/> | _bstrPageID_ &ndash; На страницу, содержащую двоичных объектов, чтобы получить идентификатор OneNote.  <br/><br/>_bstrCallBackID_ &ndash; Двоичных объектов, чтобы получить идентификатор OneNote. Этот код, известных как **callbackID**— в коде OneNote XML для страницы, возвращенный методом **GetPageContent** .  <br/><br/>_pbstrBinaryObectB64Out_ &ndash; (Выходной параметр) A указатель на строку, в которую OneNote записывает двоичный объект как строка в кодировке base 64.  <br/> |
   
### <a name="deletepagecontent-method"></a>Метод DeletePageContent

|||
|:-----|:-----|
|**Описание** <br/> |Удаляет объект &ndash; таких как объект **структуры**, **рукописного ввода**или **изображение** со страницы.  <br/> |
|**Синтаксис** <br/> | `HRESULT DeletePageContent(`<br/>`[in]BSTR bstrPageID,`<br/>`[in]BSTR bstrObjectID,`<br/>`[in,defaultvalue(0)]DATE dateExpectedLastModified,`<br/>`[in,defaultvalue(#)]VARIANT_BOOL force);` <br/> |
|**Параметры** <br/> | _bstrPageID_ &ndash; OneNote код на страницу, содержащую объект для удаления.  <br/><br/>_bstrObjectID_ &ndash; OneNote идентификатор объекта, который требуется удалить.  <br/><br/>_dateExpectedLastModified_ &ndash; (Необязательно) Дата и время, которые вы думаете, страницы, которая содержит контент, который требуется удалить Дата и время последнего изменения. Если передать ненулевое значение для этого параметра, OneNote переходит удаление только в том случае, если значение, передаваемого соответствует фактическая дата и время последнего изменения страницы. Передача значения для этого параметра предотвращает случайно перезаписи изменения, внесенные пользователями со времени последнего изменения страницы.  <br/><br/>_Принудительное_ &ndash; (Необязательно) **значение true,** чтобы обновить контент страницы, даже при наличии неизвестные данные на странице в будущих версиях OneNote; в противном случае — **значение false** (значение по умолчанию).  <br/> |
   
### <a name="publish-method"></a>Метод Publish

|||
|:-----|:-----|
|**Описание** <br/> |Экспорт в файл в любой формат, который поддерживает OneNote задаваемая страница.  <br/> |
|**Синтаксис** <br/> | `HRESULT Publish(`<br/>`[in]BSTR bstrHierarchyID,`<br/>`[in]BSTR bstrTargetFilePath,`<br/>`[in,defaultvalue(pfOneNote)]PublishFormat pfPublishFormat,`<br/>`[in,defaultvalue(0)]BSTR bstrCLSIDofExporter);` <br/> |
|**Параметры** <br/> | _bstrHierarchyID_ &ndash; OneNote идентификатор иерархии, который необходимо экспортировать.  <br/><br/>_bstrTargetFilePath_ &ndash; Абсолютный путь для расположения, где вы хотите сохранить созданный файл выходных данных. Файл должен быть еще не существует в этом расположении.  <br/><br/>_pfPublishFormat_ &ndash; Одно из значений перечисления [PublishFormat](enumerations-onenote-developer-reference.md#odc_PublishFormat) , указывает формат, в которой будут опубликованной страницы (в примере, MTHML, PDF и т.д.).  <br/><br/>_bstrCLSIDofExporter_ &ndash; Class ID (CLSID) зарегистрированных приложения COM, можно экспортировать Microsoft Windows enhanced (EMF). Приложение COM должен реализовать интерфейс **IMsoDocExporter** . Этот параметр включен, позволяющая сторонними разработчиками для создания собственных кода для публикации содержимого OneNote в пользовательском формате. Дополнительные сведения об интерфейсе **IMsoDocExporter** можно [расширение функции экспорта Фиксированная формата Office 2007](http://msdn.microsoft.com/en-us/library/office/aa338206%28v=office.12%29.aspx).  <br/> |
   
На данный момент OneNote поддерживает следующие форматы файлов:
  
- Файлы MHTML (.mht)
- Файлы Adobe Acrobat PDF (PDF-файл)
- Файлы XML Paper Specification (XPS) (с расширением XPS)
- OneNote 2013, 2010 или 2007 файлы (с расширением ONE)
- Файлы пакета OneNote (.onepkg)
- Документы Microsoft Word (DOC-файл или DOCX)
- Microsoft Windows Enhanced (EMF)
- HTML-файлы (.html)
    
При выполнении этого метода создаются точно те же результаты, которые можно получить, нажав кнопку **Опубликовать** в пользовательском Интерфейсе и Указание формата. 
  
## <a name="navigation-methods"></a>Методы навигации
<a name="ON14DevRef_Application_Navigation"> </a>

Методы, описанные в этом разделе позволяют найти, перейдите к и ссылка на записных книжек OneNote, групп разделов, разделы, страницы и объектов page.
  
### <a name="navigateto-method"></a>Метод NavigateTo

|||
|:-----|:-----|
|**Описание** <br/> |Переход на указанный объект (, например разделы, страницы и элементы **структуры** страниц).  <br/> |
|**Синтаксис** <br/> | `HRESULT NavigateTo(`<br/>`[in]BSTR bstrHierarchyObjectID,`<br/>`[in,defaultvalue(#)]BSTR bstrObjectID,`<br/>`[in,defaultvalue(0)]VARIANT_BOOL fNewWindow);` <br/> |
|**Параметры** <br/> | _bstrHierarchyObjectID_ &ndash; OneNote идентификатор объекта для перехода в иерархии OneNote.  <br/><br/>_bstrObjectID_ &ndash; OneNote идентификатор объекта, чтобы перейти на страницу OneNote.  <br/><br/>_fNewWindow_ &ndash; (Необязательно) **значение true,** чтобы открыть указанный объект в новом окне OneNote. **значение false,** не открывается новое окно OneNote Если один открыта.  <br/> |
   
### <a name="navigatetourl-method"></a>Метод NavigateToUrl

|||
|:-----|:-----|
|**Описание** <br/> |Если передается ссылка OneNote (onenote: / /), открывается окно OneNote в соответствующем расположении в OneNote. Если ссылки является внешним по отношению OneNote (например, http:// или file://), будет отображаться диалоговое окно безопасности. После увольнение OneNote пытается открыть ссылку и возвращается сообщение об ошибке **HResult.hrObjectDoesNotExist** .  <br/> |
|**Синтаксис** <br/> | `HRESULT NavigateTo(`<br/>`[in]BSTR bstrUrl,`<br/>`[in,defaultvalue(0)]VARIANT_BOOL fNewWindow);` <br/> |
|**Параметры** <br/> | _bstrUrl_ &ndash; Строку, которая указывает место для перехода. Это может быть ссылка OneNote или другие URL, например web ссылку или в сетевую папку.  <br/><br/>_fNewWindow_ &ndash; (Необязательно) **значение true,** чтобы открыть указанный URL-адрес в новом окне OneNote. **значение false,** не открывается новое окно OneNote Если один открыта.  <br/> |
   
### <a name="gethyperlinktoobject-method"></a>Метод GetHyperLinkToObject

|||
|:-----|:-----|
|**Описание** <br/> |Получает гиперссылка OneNote на указанный записной книжки, группы раздела, раздел, страницу или объект page.  <br/> |
|**Синтаксис** <br/> | `HRESULT GetHyperlinkToObject(`<br/>`[in] BSTR bstrHierarchyID,`<br/>`[in] BSTR bstrPageContentObjectID,`<br/>`[out] BSTR * pbstrHyperlinkOut);` <br/> |
|**Параметры** <br/> | _bstrHierarchyID_ &ndash; Идентификатор OneNote для записной книжки, группы раздела, раздел или страницы, для которого вы хотите гиперссылки.  <br/><br/>_bstrPageContentObjectID_ &ndash; (Необязательно) OneNote идентификатор объекта в страницы, для которого вы хотите гиперссылки. Например объект может быть структуры или элемент **структуры** . Если передать пустую строку ("»), возвращаемых ссылка указывает записной книжки, раздел группы раздела или страницы, указанный в параметре _bstrHierarchyID_ . При передаче пустой строке для параметра _bstrPageContentObjectID_ параметр _bstrHierarchyID_ должен быть ссылку на страницу, содержащую указанный объект.  <br/><br/>_pbstrHyperlinkOut_ &ndash; (Выходной параметр) A указатель на строку, в которую метод **GetHyperlinkToObject** записывает текст гиперссылки выходные данные.  <br/> |
   
При попытке перейдите к конечной ссылки, OneNote открывает и отображает указанный объект в браузере.
  
### <a name="getwebhyperlinktoobject-method"></a>Метод GetWebHyperlinktoObject

|||
|:-----|:-----|
|**Описание** <br/> |Возвращает объект, который будет открыта в веб-клиент OneNote гиперссылки.  <br/> |
|**Синтаксис** <br/> | `HRESULT GetWebHyperlinkToObject (`<br/>`[in] BSTR bstrHierarchyID,`<br/>`[in] BSTR bstrPageContentObjectID,`<br/>`[out] BSTR * pbstrHyperlinkOut);` <br/> |
|**Параметры** <br/> | _bstrHierarchyID_ &ndash; Идентификатор OneNote для записной книжки, раздел группы, раздел или страницы, для которого вы хотите гиперссылки.  <br/><br/>_bstrPageContentObjectID_ &ndash; (Необязательно) OneNote идентификатор объекта в страницы, для которого вы хотите гиперссылки. Например объект может быть структуры или элемент **структуры** . Если передать пустую строку ("»), возвращаемых ссылка указывает записной книжки, раздел группы раздела или страницы, указанный в параметре _bstrHierarchyID_ . При передаче пустой строке для параметра _bstrPageContentObjectID_ параметр _bstrHierarchyID_ должен быть ссылку на страницу, содержащую указанный объект.  <br/><br/>_pbstrHyperlinkOut_ &ndash; (Выходной параметр) A указатель на строку, в которую метод **GetWebHyperlinkToObject** записывает текст гиперссылки выходные данные. Если не удается создать гиперссылку web для записной книжки, возвращается значение null.  <br/> |
   
### <a name="findpages-method"></a>Метод FindPages

|||
|:-----|:-----|
|**Описание**|Возвращает список страниц, соответствующие указанному запросу терминов.|
|**Синтаксис**| `HRESULT FindPages(`<br/>`[in]BSTR bstrStartNodeID,`<br/>`[in]BSTR bstrSearchBSTR,`<br/>`[out]BSTR * pbstrHierarchyXmlOut,`<br/>`[in,defaultvalue(#)]VARIANT_BOOL fIncludeUnindexedPages,`<br/>`[in,defaultvalue(0)]VARIANT_BOOL fDisplay,`<br/>`[in,defaultvalue(#)]XMLSchema xsSchema);`|
|**Параметры**| _bstrStartNodeID_ &ndash; Узел (корневой, записной книжки, раздел группы или раздела) ниже для поиска содержимого. Этот параметр задает область поиска.<br/><br/>_bstrSearchString_ &ndash; Строке поиска. Передайте именно то же, следует ввести в поле «Поиск» в пользовательском Интерфейсе OneNote. Можно использовать битовые операторы, такие как **и** и **или**, должна быть прописными буквами.<br/><br/>_pbstrHierarchyXmlOut_ &ndash; (Выходной параметр) A указатель на строку, в которую необходимо OneNote для записи выходного XML-строка. Итоговый XML-строка содержит иерархию записной книжки из корневого каталога вниз, включая, любых страниц, которые соответствуют строке поиска. Например метод **FindPages** не позволяет отобразить список разделам, не имеющих страницы совпадений в иерархии. Кроме того если только одна страница в одном разделе соответствует строке, возвращенные иерархией включает путь для этого раздела и страницы, но для других частей в иерархии записной книжки.<br/><br/>_fIncludeUnindexedPages_ &ndash; (Необязательно) **значение true** для поиска по страницам, которые не были индексированы программой поиска в Windows; в противном случае — **false**.<br/><br/>_fDisplay_ &ndash; (Необязательно) **значение true,** также выполнение поиска в пользовательском Интерфейсе для пользователей, как если бы пользователь ввел его сами. **значение false** для выполнения запросов без изменения в пользовательский интерфейс (по умолчанию).<br/><br/>_xsSchema_ &ndash; The OneNote (необязательно) версия схемы строка _pbstrHierarchyXmlOut_. Это необязательный значение используется для указания версии OneNote XML-схему, содержащая строку _pbstrHierarchyXmlOut_ . Если это значение не указано, OneNote будет предполагать, что XML-код находится в версии схемы _xsCurrent_. <br/><br/>**Примечание**: рекомендуется использовать указание версии OneNote (например, **xs2013**), а не с помощью **xsCurrent** или оставить пустым, так как это позволит надстройки для работы с будущими версиями OneNote.           |
   
 **FindPages** работает только в том случае, если у вас есть Microsoft Search 3.0 или 4.0, установленный на компьютере. ОС Windows Vista и Windows 7 включают этот компонент. Тем не менее если выполняется более ранней версии Windows, необходимо установить **FindPages** для работы [Службы поиска Windows](http://www.microsoft.com/windows/products/winfamily/desktopsearch/getitnow.mspx) . 
  
### <a name="findmeta-method"></a>Метод FindMeta

|||
|:-----|:-----|
|**Описание**|Возвращает список объектов OneNote, которые содержат метаданных, которая соответствует указанному запросу терминов.|
|**Синтаксис**| `HRESULT FindMeta (`<br/>`[in]BSTR bstrStartNodeID,`<br/>`[in]BSTR bstrSearchBSTRName,`<br/>`[out]BSTR * pbstrHierarchyXmlOut,`<br/>`[in,defaultvalue(#)]VARIANT_BOOL fIncludeUnindexedPages,`<br/>`[in,defaultvalue(#)]XMLSchema xsSchema);`|
|**Параметры**| _bstrStartNodeID_ &ndash; Узел (корневой, записной книжки, раздел группы или раздела) ниже для поиска содержимого. Этот параметр задает область поиска.<br/><br/>_bstrSearchStringName_ &ndash; Строке поиска. Передайте в любой части имени метаданных. Если передать в пустую строку или значение null, все объекты, имеющие метаданных будет соответствовать запрос.<br/><br/>_pbstrHierarchyXmlOut_ &ndash; (Выходной параметр) A указатель на строку, в которую необходимо OneNote для записи выходного XML-строка. Итоговый XML-строка содержит иерархию записной книжки из корневого каталога вниз, включая, любых страниц, которые соответствуют строке поиска. Например метод **FindPages** не позволяет отобразить список разделам, не имеющих страницы совпадений в иерархии. Кроме того если только одна страница в одном разделе соответствует строке, возвращенные иерархией включает путь для этого раздела и страницы, но для других частей в иерархии записной книжки.  <br/><br/>_fIncludeUnindexedPages_ &ndash; (Необязательно) **значение true** для поиска по страницам, которые не были индексированы программой поиска в Windows; в противном случае — **false**.<br/><br/>_xsSchema_ &ndash; The OneNote (необязательно) версия схемы строка _pbstrHierarchyXmlOut_. Это необязательный значение используется для указания версии OneNote XML-схему, содержащая строку _pbstrHierarchyXmlOut_ . Если это значение не указано, OneNote будет предполагать, что XML-код находится в версии схемы _xsCurrent_. <br/><br/>**Примечание**: рекомендуется использовать указание версии OneNote (например, **xs2013**), а не с помощью **xsCurrent** или оставить пустым, так как это позволит надстройки для работы с будущими версиями OneNote.           |
   
**FindMeta** работает только в том случае, если у вас есть Microsoft Windows Search 3.0 или 4.0, установленный на компьютере. ОС Windows Vista и Windows 7 включают этот компонент. Тем не менее если выполняется более ранней версии Windows, необходимо установить **FindMeta** для работы [Службы поиска Windows](http://www.microsoft.com/windows/products/winfamily/desktopsearch/getitnow.mspx) . 
  
## <a name="functional-methods"></a>Методы работы
<a name="ON14DevRef_Application_Functional"> </a>

Методы, описанные в этом разделе позволяют выполнять определенные действия или задать параметры в приложении OneNote.
  
### <a name="mergefiles-method"></a>Метод MergeFiles

|||
|:-----|:-----|
|**Описание** <br/> |Пользователи могут объединить изменения для одного файла в одну. Для файлов, которые следует учитывать то же самое они должны иметь тот же идентификатор OneNote.  <br/> |
|**Синтаксис** <br/> | `HRESULT MergeFiles (`<br/>`[in]BSTR bstrBaseFile,`<br/>`[in]BSTR bstrClientFile,`<br/>`[in]BSTR bstrServerFile,`<br/>`[in]BSTR bstrTargetFile);` <br/> |
|**Параметры** <br/> | _bstrBaseFile_ &ndash; Строку пути к месту расположения файла с расширением ONE of исходное состояние файла.  <br/><br/>_bstrClientFile_ &ndash; Строку пути к месту расположения файла .one второго набора изменений для объединения с базового файла после изменения файлов серверов объединяются с базового.  <br/><br/>_bstrServerFile_ &ndash; Строку пути к месту расположения файла .one первого набора изменений для объединения с базового файла.  <br/><br/>_bstrTargetFile_ &ndash; Строку пути к месту расположения файла .one файла со слиянием изменений.  <br/> |
   
Метод **MergeFiles** предназначена для мобильных устройств сценарии, в которых может существовать несколько версий файла OneNote. 
  
### <a name="mergesections-method"></a>Метод MergeSections

|||
|:-----|:-----|
|**Описание** <br/> |Объединяет содержимое из одного раздела в другой в OneNote.  <br/> |
|**Синтаксис** <br/> | `HRESULT MergeSections (`<br/>`[in]BSTR bstrSectionSourceId,`<br/>`[in]BSTR bstrSectionDestinationId);` <br/> |
|**Параметры** <br/> | _bstrSectionSourceId_ &ndash; OneNote идентификатор раздела для объединения.  <br/><br/>_bstrSectionDestinationId_ &ndash; Идентификатор раздела для объединения в OneNote. В разделе источник будут объединены в этом разделе назначения.  <br/> |
   
Этот метод выполняет ту же операцию, как **Объединить в другой раздел** компонент, который будет отображаться при щелчке правой кнопкой мыши раздел. 
  
### <a name="quickfiling-method"></a>Метод quickfiling (en)

|||
|:-----|:-----|
|**Описание** <br/> |Возвращает экземпляр диалогового окна [IQuickFilingDialog](quick-filing-dialog-box-interfaces-onenote.md#odc_IQuickFilingDialog) , которую можно использовать для выбора расположения в дереве иерархии OneNote.  <br/> |
|**Синтаксис** <br/> | `HRESULT QuickFiling (`<br/>` );` <br/> |
   
### <a name="synchierarchy-method"></a>Метод SyncHierarchy

|||
|:-----|:-----|
|**Описание** <br/> |Выполняет принудительное синхронизации на указанный объект с исходного файла на диске.  <br/> |
|**Синтаксис** <br/> | `HRESULT SyncHierarchy (`<br/>`[in]BSTR bstrHierarchyID);` <br/> |
|**Параметры** <br/> | _bstrHierarchyID_ &ndash; OneNote идентификатор объекта должны быть синхронизированы.  <br/> |
   
### <a name="setfilinglocation-method"></a>Метод SetFilingLocation

|||
|:-----|:-----|
|**Описание** <br/> |Пользователи могут указать, где и как некоторых типов содержимого должен быть оформлена в OneNote.  <br/> |
|**Синтаксис** <br/> | `HRESULT SetFilingLocation (`<br/>`[in]FilingLocation flToSet,`<br/>`[in]FilingLocationType fltToSet,`<br/>`[in]BSTR bstrFilingSectionID);`           <br/> |
|**Параметры** <br/> | _flToSet_ &ndash; Объекта тип места хранения для установки.  <br/><br/>_fltToSet_ &ndash; Место для типа файлов.  <br/><br/>_bstrFilingSectionID_ &ndash; OneNote идентификатор раздела или страницы, в какой расположении требуется установить. Если не применимо, пользователь может передавать в null или пустую строку.  <br/> |
   
Типы контента, который может быть оформлена включают элементов Outlook и веб-заметок с помощью Internet Explorer, который требуется импортировать в OneNote посредством команды **Отправить в OneNote** в каждом приложении. Место хранения элементов, которые являются при выводе на печать в OneNote также можно установить с помощью этого метода. 
  
## <a name="properties"></a>Свойства
<a name="ON14DevRef_Application_Properties"> </a>

В этом разделе описаны свойства интерфейса **приложения** . 
  
|**Свойство**|**Описание**|
|:-----|:-----|
|**Windows** <br/> |Предоставляет пользователям доступ к открытых окнах OneNote. Это свойство позволяет пользователям нумерации внутри набора OneNote windows и изменении окно свойств. Для получения дополнительных сведений см [Интерфейсов Windows](window-interfaces-onenote.md).  <br/> |
|**COMAddIns** <br/> |Возвращает коллекцию **COMAddIns** для OneNote. Эта коллекция содержит все COM-надстроек, доступных в OneNote. Свойство **Count** коллекции **COMAddins** возвращает число доступных COM-надстроек. Дополнительные сведения см в объекте [COMAddIns](http://msdn.microsoft.com/en-us/library/office/ff865489.aspx) .  <br/> |
|**LanguageSettings** <br/> |Предоставляет доступ к некоторые интерфейсы API для изменения стандартных настроек языка из OneNote.  <br/> |
   
## <a name="events"></a>Events
<a name="ON14DevRef_Application_Events"> </a>

В этом разделе описываются события интерфейса приложения.
  
> [!CAUTION]
> События в настоящее время не удается добавить в управляемом коде. 
  
### <a name="onnavigate-event"></a>Событие OnNavigate

|||
|:-----|:-----|
|**Описание** <br/> |Позволяет пользователю назначить функция, вызываемая при переходе от текущего расположения OneNote пользовательского интерфейса OneNote.  <br/> |
|**Синтаксис** <br/> | `Event OnNavigate (`<br/>` );` <br/> |
   
### <a name="onhierarchychange-method"></a>Метод OnHierarchyChange

|||
|:-----|:-----|
|**Описание** <br/> |Позволяет пользователю назначить функция, вызываемая каждом изменении OneNote иерархии (например, для добавления или удаления страниц или перемещение разделов). Изменения иерархии являются пакетными, поэтому в случае несколько изменений в или рядом и то же время OneNote вызывает событие один раз.  <br/> |
|**Синтаксис** <br/> | `Event OnHierarchyChange (`<br/>` BSTR bstrActivePageID);` <br/> |
|**Параметры** <br/> | _bstrActivePageID_ &ndash; Передает идентификатор OneNote на активной странице.  <br/> |
   
## <a name="see-also"></a>См. также

- [Справочник разработчика по OneNote](onenote-developer-reference.md)

